name: Deploy static public site to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Validate and deploy site/dist
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Build static site
        run: python site/build.py

      - name: Check generated public data summaries
        run: python scripts/generate_public_data_summaries.py --check

      - name: Check compatibility surfaces
        run: python scripts/generate_compatibility_surfaces.py --check

      - name: Check static resolver demos
        run: python scripts/generate_static_resolver_demos.py --check

      - name: Validate publication inventory
        run: python scripts/validate_publication_inventory.py

      - name: Validate static public site
        run: python site/validate.py

      - name: Check GitHub Pages static artifact
        run: python scripts/check_github_pages_static_artifact.py --path site/dist

      - name: Check static site generated artifact drift
        run: python scripts/check_generated_artifact_drift.py --artifact static_site_dist

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload static public site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/dist

      - name: Deploy static public site to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
