# Search Interaction Contracts

These contracts define search requests, compiled queries, resolution runs, result lanes, partial results, candidate clusters, action posture, controls, feedback, plan patches, absence, coverage, and discovery trails. They are contract/planning artifacts only and do not implement runtime search behavior.
