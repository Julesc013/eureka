
# Capability Contracts

Capability contracts define read-only projection negotiation for snapshot relays. Profiles advertise supported packet/projection versions and explicitly disable live source actions, review, mutation, downloads, and extraction.
