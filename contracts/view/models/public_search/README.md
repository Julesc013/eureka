# Public Search View Models

Canonical public search view-model contracts live here. This root extends the
existing `contracts/view/models` authority and does not create a duplicate
`contracts/views` authority.

These contracts define public/search semantics only. They do not authorize
deployment, public launch, mutation, downloads, extraction, live source fanout,
model calls, or candidate promotion.
