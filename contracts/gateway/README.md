# Gateway Contracts

`contracts/gateway/` holds governed public API contracts for the gateway-facing product boundary.

These files define what surfaces may rely on in the normal path. They are placeholders during bootstrap and should not be mistaken for finalized product promises.

