$schema: https://json-schema.org/draft/2020-12/schema
$id: https://schemas.eureka.invalid/archive/object.schema.yaml
$comment: Provisional draft skeleton only. Not yet a finalized semantic contract or compatibility promise.
title: Object
description: Draft canonical subject record for a governed archive object.
type: object
additionalProperties: false
properties:
  id:
    type: string
    description: Draft canonical identifier for the object record.
  kind:
    type: string
    description: Draft object classification label.
  label:
    type: string
    description: Optional human-readable summary label.
required:
  - id
x-eureka-status: draft
