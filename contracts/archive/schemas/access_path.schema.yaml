$schema: https://json-schema.org/draft/2020-12/schema
$id: https://schemas.eureka.invalid/archive/access_path.schema.yaml
$comment: Provisional draft skeleton only. Not yet a finalized semantic contract or compatibility promise.
title: AccessPath
description: Draft canonical record for a way to reach or obtain an object, representation, or evidence source.
type: object
additionalProperties: false
properties:
  id:
    type: string
    description: Draft canonical identifier for the access path record.
  kind:
    type: string
    description: Draft access path classification label.
  locator:
    type: string
    description: Draft location or lookup token.
required:
  - id
x-eureka-status: draft
