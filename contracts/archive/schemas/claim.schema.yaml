$schema: https://json-schema.org/draft/2020-12/schema
$id: https://schemas.eureka.invalid/archive/claim.schema.yaml
$comment: Provisional draft skeleton only. Not yet a finalized semantic contract or compatibility promise.
title: Claim
description: Draft canonical statement about an archive subject that may be supported or challenged by evidence.
type: object
additionalProperties: false
properties:
  id:
    type: string
    description: Draft canonical identifier for the claim record.
  subject_id:
    type: string
    description: Draft identifier of the subject this claim refers to.
  predicate:
    type: string
    description: Draft assertion label.
  value:
    description: Draft claimed value. The final value model remains unresolved.
required:
  - id
x-eureka-status: draft
