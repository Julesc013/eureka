# Archive Protocols

Protocols are first-class governed assets alongside schemas. Bootstrap starts with descriptive placeholders only.

Any future protocol here should explain what moves between components or operators without encoding runtime implementation detail into connector code.

