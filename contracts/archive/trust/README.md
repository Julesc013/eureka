# Archive Trust

Trust semantics belong under governed archive contracts, not under connectors.

Bootstrap does not finalize the trust model. This directory exists so the trust boundary is explicit from the start.
