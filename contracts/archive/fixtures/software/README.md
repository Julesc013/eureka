# Synthetic Software Fixtures

This directory contains small, synthetic, rights-safe software fixtures for the bootstrap thin slice.

Current fixture scope:

- one exact-match synthetic software target
- one release or version marker expressed through the target reference and state record
- one representation marker
- one access-path marker pointing back to the governed local fixture set

No real software payload, network acquisition, or connector behavior is included here.

