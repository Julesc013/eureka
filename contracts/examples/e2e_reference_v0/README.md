# E2E Reference v0 Synthetic Examples

This directory contains a single linked synthetic example chain for
`E2E-REFERENCE-CONTRACT-00`.

The example is:

- synthetic only;
- non-authoritative;
- not an IA candidate;
- not a production reviewed record;
- not public eligible;
- not evidence of download, rights, malware safety, or artifact verification.

The example exists so contract and architecture tests can verify the reference
chain, stable semantic hashes, authority labels, and forbidden truth-boundary
transitions without mutating production stores.

