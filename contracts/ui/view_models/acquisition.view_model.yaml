$schema: https://json-schema.org/draft/2020-12/schema
$id: https://schemas.eureka.invalid/ui/acquisition.view_model.yaml
$comment: Provisional draft skeleton only. Shared across web and native surfaces in the normal path.
title: Acquisition
description: Draft shared bounded acquisition and fetch model aligned with the gateway public acquisition boundary.
type: object
additionalProperties: false
properties:
  status:
    type: string
    enum:
      - fetched
      - unavailable
      - blocked
  acquisition_status:
    type: string
    enum:
      - fetched
      - unavailable
      - blocked
  target_ref:
    type: string
  representation_id:
    type: string
  resolved_resource_id:
    type: string
  representation_kind:
    type: string
  label:
    type: string
  filename:
    type: string
  content_type:
    type: string
  byte_length:
    type: integer
    minimum: 0
  source_family:
    type: string
  source_label:
    type: string
  source_locator:
    type: string
  access_kind:
    type: string
  access_locator:
    type: string
  reason_codes:
    type: array
    items:
      type: string
  reason_messages:
    type: array
    items:
      type: string
  notices:
    type: array
    items:
      $ref: '#/$defs/notice'
required:
  - status
  - acquisition_status
  - target_ref
  - representation_id
  - reason_codes
  - reason_messages
  - notices
$defs:
  notice:
    type: object
    additionalProperties: false
    properties:
      code:
        type: string
      severity:
        type: string
      message:
        type: string
    required:
      - code
      - severity
x-eureka-status: draft
