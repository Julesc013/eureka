# Public Site Assets

This directory contains static assets for `public_site/`.

`site.css` is plain CSS. The static public site pack does not require
JavaScript, a build step, a package manager, or network access.
