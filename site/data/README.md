# Site Data

This directory is reserved for static-site generator inputs that are neither
page JSON nor templates.

Static Site Generation Migration v0 keeps generated public data summaries
deferred. Future Generated Public Data Summaries v0 should add governed
machine-readable files under `/data/` without live data or external baseline
observations unless repo evidence exists.
