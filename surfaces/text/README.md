# Text Surface

`surfaces/text/` records the plain-text projection family used by the committed
static site artifact under `site/dist/text/`.

The generated files remain generated site output. This directory does not own a
separate text runtime or public search behavior.
