# Lite Surface

`surfaces/lite/` records the lightweight static projection family used by the
committed static site artifact under `site/dist/lite/`.

The generated files remain generated site output. This directory does not claim
hosted application maturity or a separate runtime.
