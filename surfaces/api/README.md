# API Surface

`surfaces/api/` records API projection ownership only.

Runtime service behavior remains under `runtime/gateway/`. This directory must
not become a second gateway, own engine behavior, or claim hosted API maturity.
