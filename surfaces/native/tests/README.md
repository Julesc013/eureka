# Native Tests

This directory is for component-local native tests.

Local-mode behavior should remain explicitly bounded if it is introduced later. Bootstrap does not imply that local engine embedding already exists.

