# Files Surface

`surfaces/files/` records the downloadable/static file-list projection family
used by the committed static site artifact under `site/dist/files/`.

The generated files remain generated site output. This directory does not own a
release engine, installer behavior, or download automation.
