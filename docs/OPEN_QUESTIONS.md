# Open Questions

These questions are intentionally left open during bootstrap:

1. What exact object identity boundary should Eureka use for composite software artifacts?
2. Which trust semantics need to be normative in `contracts/archive/trust` for v1?
3. How narrow should the future `runtime/engine/sdk` exposure be if native offline mode exists at all?
4. Which gateway operations belong in the public API versus internal runtime protocols?
5. Which draft job statuses, notices, and result envelope fields should become durable public compatibility promises?
6. Which shared UI state should be standardized across web and native without over-constraining either surface?
7. What is the first durable versioning strategy for archive contracts, gateway contracts, and migration metadata?
8. How far should the current narrow Python import checker grow beyond the bootstrap rules it enforces today, and which additional boundaries are worth checking before it becomes noisy or overcommitted?
9. After the first recorded GitHub Releases connector slice, which real source family should follow next, and which normalized fields should stay stable across source families?
10. If job execution later becomes asynchronous, which parts of the current bounded job envelope should remain stable?
11. Should submit and read remain one shared public envelope shape, or should they diverge into distinct durable contract types before the current local bootstrap HTTP API is expanded into any durable network-facing contract?
12. How long should the web workbench remain compatibility-first and server-rendered before any browser-side behavior is introduced, and which view-model fields must stay stable when that happens?
13. Which deterministic search fields and ordering guarantees should survive as the bounded corpus grows through additional recorded fixture families?
14. Which parts of the bootstrap `resolution_manifest` should survive into a durable manifest or snapshot contract, and which parts should remain explicitly local-export metadata only?
15. Should future bounded actions hang directly off `target_ref`, off read-job results, or off a more explicit resolved-resource identifier once the public API grows beyond the current local deterministic slice?
16. Which parts of the bootstrap `resolution_bundle` should survive into a durable portable bundle or snapshot contract, and which parts should remain explicitly local-export packaging only?
17. When the bundle inspection path stops using a local filesystem path for demos, what durable upload or import boundary should replace it without overcommitting restore semantics?
18. Which local store semantics should remain durable if the bootstrap content-addressed store later grows into a broader cache or database layer, and which parts should stay explicitly demo-only?
19. Which inputs should continue to participate in bootstrap `resolved_resource_id` derivation as more real external sources arrive after GitHub Releases, and which parts of that derivation should remain local implementation detail rather than contract?
20. At what point should `resolved_resource_id` become a first-class public compatibility promise rather than a bootstrap deterministic seam carried opportunistically through current envelopes?
21. How should the current local `/api/...` bootstrap routes relate to the draft `/v0/...` public API contract paths if Eureka later promotes this slice beyond a local stdlib proof?
22. Which bounded evidence-summary fields introduced in the current bootstrap seam should survive into a durable provenance or claim contract, and which should remain explicitly replaceable implementation detail?
23. Which bounded comparison categories introduced in the current bootstrap seam should survive into a durable side-by-side comparison contract, and which should remain explicitly local and replaceable before any future merge or truth-selection work?
24. Which parts of the current bootstrap `subject_key` grouping plus deterministic state-ordering rule should survive into a durable object/state identity contract, and which parts should remain explicitly replaceable before any broader temporal or merge semantics are introduced?
25. Which parts of the current bounded absence reports, near-match hints, and reason codes should survive into a durable diagnostic contract, and which should remain explicitly replaceable before any future ranking, trust, or broader reasoning work?
26. Which parts of the current bounded representation and access-path summaries should survive into a durable representation or action contract, and which parts should remain explicitly replaceable before any future download, install, import, or restore semantics are introduced?
27. Which parts of the current bounded host profile presets, requirement hints, and compatibility reason codes should survive into a durable compatibility contract, and which should remain explicitly replaceable before any future installer, runtime-routing, or broader host-detection work?
28. Which parts of the current bounded action-plan statuses, reason codes, and recommendation order should survive into a durable action-routing contract, and which should remain explicitly replaceable before any future execution, installer, or workflow-policy work?
29. Which parts of the current bounded strategy profiles, emphasis hints, and strategy-aware recommendation changes should survive into a durable user-intent contract, and which should remain explicitly replaceable before any future personalization, ranking, or broader user-model work?
30. Which parts of the current bounded representation-selection statuses, reason codes, and handoff-preference rules should survive into a durable handoff contract, and which should remain explicitly replaceable before any future installer, runtime-launch, or orchestration work?
31. Which parts of the current bounded acquisition result statuses, reason codes, and local fixture-backed fetch semantics should survive into a durable acquisition contract, and which should remain explicitly replaceable before any future live download, installer, or execution work?
32. Which parts of the current bounded decomposition statuses, ZIP member summaries, and unsupported-format reasoning should survive into a durable package or container inspection contract, and which should remain explicitly replaceable before any broader extractor, installer, import, or restore work?
33. Which parts of the current bounded member-access statuses, preview rules, and ZIP-only member readback semantics should survive into a durable member-readback contract, and which should remain explicitly replaceable before any broader extractor, installer, import, or restore work?
34. Should the current exact-resolution plus deterministic-search seams eventually converge into a resumable investigation-run contract with explicit phases, budgets, checkpoints, and remaining-work summaries, or should that stay engine-private even if the runtime grows more asynchronous later?
35. If Eureka later introduces shared evidence reuse or resolution memory, which units are safe to share publicly, which must stay local private strategy memory, and where should invalidation rules and freshness dependencies live?
36. Now that Archive Resolution Eval Runner v0 executes the hard archive-resolution packet, how should the benchmark evolve beyond v0 check statuses, and which metrics should eventually become the minimum gate for future investigation, ranking, decomposition, compatibility, source-expansion, and absence claims?
37. Which future planning, extraction, explanation, or reranking tasks should remain deterministic, which may use lightweight ML, and which, if any, should allow optional LLM fallbacks without turning model output into product truth?
38. Which Source Registry v0 fields should now stay stable enough for future public projection, and which should remain explicitly descriptive and replaceable rather than becoming premature compatibility promises?
39. Which additional source-registry seed records should be added beyond the current v0 set, and what minimum runtime or documentation bar should a placeholder source meet before it is promoted into an implemented connector family?
40. Now that Resolution Run Model v0 persists synchronous local JSON run records, which fields should remain durable as future planner, phase, checkpoint, or worker semantics arrive, and which should stay explicitly replaceable?
41. Should future run kinds remain limited to `exact_resolution`, `deterministic_search`, and planner-backed `planned_search`, or is there a bounded case for adding `subject_states` before indexing or worker work lands?
42. Which additional deterministic query families should follow Query Planner v0 beyond the current eval-backed set, and what evidence bar should a family meet before it becomes accepted planner behavior?
43. Which Local Index v0 schema details should remain stable enough for public projection, and which should stay explicitly bootstrap-local implementation detail while SQLite remains the first local index substrate?
44. Which additional indexed record families or fields should follow the current v0 set of resolved-object, state-or-release, representation, member, evidence, and source-record summaries without prematurely turning the index into a ranking or extraction engine?
45. Which Local Worker and Task Model v0 fields should remain durable if later worker queues, retries, priorities, checkpoints, or distributed orchestration arrive, and which should stay explicitly replaceable bootstrap-local detail?
46. Now that Resolution Memory v0 exists, which fields should remain local-private by default, which parts of public source-derived memory may later become shareable, and what invalidation boundary should exist before any broader reuse is attempted?
47. Which Public Alpha Safe Mode v0 route-policy decisions should become durable alpha contract, and which blocked local-path, bundle, fetch, task, memory, index, or store behaviors should later gain configured server-owned alternatives?
48. What should the Rust parity strategy require before any runtime seam is considered safely migrated out of the Python reference lane?
49. At what milestone should thin native host-shell experiments become worthwhile, and what backend or public-boundary conditions must be true first?
50. Which additional synchronous local task kinds should follow the current v0 set without prematurely turning the task substrate into a production scheduler or queue system?
51. Should Archive Resolution Eval Runner v0 become a local task kind later, and if so how should that remain synchronous and local without implying background benchmark workers or production scheduling?
52. Now that Public Alpha Deployment Readiness Review v0 inventories routes and adds smoke checks, which review-required route groups should be promoted, further restricted, or removed before a real public-alpha demo?
53. After Public Alpha Hosting Pack v0, which checklist failures should be hard rehearsal blockers versus advisory warnings, and who owns that operator decision outside the repo?
54. What evidence would be required to convert the supervised public-alpha rehearsal packet into a real hosted-alpha gate without adding deployment infrastructure, auth, TLS, accounts, rate limiting, or production process management inside this repo?
55. Now that Rust Parity Fixture Pack v0 captures the first Python-oracle outputs, which fields should become strict byte-for-byte parity requirements and which should gain explicit normalization rules before Rust candidate seams appear?
56. Now that `eureka-core` contains the first Rust source-registry parity candidate, should the next candidate seam be Query Planner v0 or a tiny reusable Rust parity-output harness? Answered for v0 by Rust Query Planner Parity Candidate v0: add an isolated deterministic Rust query-planner candidate plus a compact Python-oracle case map and stdlib parity structure check, while keeping Cargo optional and leaving Rust unwired from runtime behavior.
57. Who accepts allowed divergence records when future Rust candidate output differs from Python oracle output?
58. Which source-registry fields should graduate from parity fixture shape into longer-lived Rust contract types, and which should remain implementation detail? Answered for v0 by Rust Source Registry Parity Catch-up v0: model the current source id, family, status/posture, connector mode, coverage depth/status, capability booleans, limitations, next coverage step, and placeholder warning fields in the isolated Rust candidate while keeping Python as the oracle and treating future contract promotion as separate work.
59. Now that Search Usefulness Audit v0 reports broad source, planner, decomposition, representation, compatibility, and UX gaps, which failure family should become the next bounded implementation target?
60. What process should govern manual Google, Internet Archive metadata, and Internet Archive OCR/full-text baseline observations so they remain evidence records rather than scraped or fabricated comparisons? Answered for v0 by Manual External Baseline Observation Pack v0: require manual-only systems, operator/timestamp/query metadata, bounded usefulness scores, validation, and pending slots that cannot count as observed.
61. Which audit findings from Comprehensive Test/Eval Operating Layer and Repo Audit v0 should become required hard tests before broader source, planner, public-alpha, or Rust parity work proceeds?
62. Should `control/inventory/tests/test_registry.json` become the source of truth for future AIDE command metadata, or should it remain a curated verification registry beside `.aide/commands/`?
63. What minimum evidence should be required before an audit finding can be marked resolved rather than merely deferred or superseded?
64. Which additional hard tests should graduate into Hard Test Pack v1 after the first source-coverage, planner-gap, or Rust parity follow-up changes land?
65. How should hardening tests be updated when a real source pack legitimately turns a current capability gap into a covered result without weakening the underlying hard eval fixture?
66. Which source capability fields are stable enough to become governed Source Registry contract fields, and which should remain descriptive triage metadata for now?
67. What minimum recorded fixture evidence is needed before an old-platform query may move from `source_gap` to `partial` or `covered` in Search Usefulness Audit results?
68. What member target-ref shape should be stable enough for parent lineage, member-level index records, and later Rust parity fixtures?
69. After Source Coverage and Capability Model v0, which recorded fixture families should be sufficient to promote Internet Archive or Wayback/Memento placeholder records into active recorded-fixture sources without implying live access?
70. Which coverage-depth changes should require Python-oracle golden fixture updates, and which can remain descriptive inventory-only changes?
71. How should future Rust source-registry parity candidates handle capability and coverage fields once Cargo checks become part of a maintained optional lane?
72. Which old-platform planner hint fields should become stable public/planner contract fields before Rust Query Planner Parity Candidate v0, and which should remain replaceable Python-oracle implementation detail?
73. How should member paths from committed local bundle fixtures graduate into stable member target refs without implying arbitrary local filesystem ingestion?
74. Now that bounded result lanes and user-cost hints exist, which fields should become durable public compatibility promises, and which should remain replaceable until broader result-lane and source-evidence work matures?
75. Which first compatibility evidence fields should be added for Windows 98, Windows 2000, Windows XP, Vista, Windows 7 / NT 6.1, x86/x64, installer/portable/driver distinctions, and unknown compatibility without turning Eureka into a compatibility oracle?
76. Now that Compatibility Evidence Pack v0 exists, which evidence fields, confidence labels, locator shapes, and unknown-compatibility outcomes should become stable enough for future Rust parity and public API compatibility promises?
77. Which recorded old-platform source fixtures should Old-Platform Source Coverage Expansion v0 add first so that Windows 7/XP/2000/98 utility, driver, support-media, manual, and latest-compatible release queries can move only when source evidence supports the status change?
78. What recurring machine-derived Search Usefulness Audit baseline format should replace historical reported aggregate baselines for future delta packs?
79. After Old-Platform Source Coverage Expansion v0 moved the current audit to 20 partial results and 28 source gaps, should the next implementation focus on compatibility evidence expansion, result-lane refinement, another recorded source pack, or Rust planner parity?
80. For Old-Platform Result Refinement Pack v0, which deterministic lane-placement and bad-result pattern checks are strict enough to let archive-resolution hard eval partials become satisfied without becoming production ranking?
81. After Old-Platform Result Refinement Pack v0 satisfied the driver support-CD hard eval but left four old-platform tasks partial, which recorded fixture evidence should More Source Coverage Expansion v1 add first: exact latest-compatible release evidence, concrete vague-identity product evidence, direct artifact/member evidence, or article/scan fixtures? Answered for v1: target the four old-platform partials first and leave article/scan for a dedicated pack.
82. What is the smallest bounded article/page/scan fixture shape that can move `article_inside_magazine_scan` without implying live OCR, broad scan search, or scraped Internet Archive evidence? Answered for v0: a separate `article-scan-recorded-fixtures` source with one synthetic article segment, parent issue lineage, page range, and OCR-like fixture text is sufficient.
83. What manual observation workflow should compare current Eureka results against Google, Internet Archive metadata search, and Internet Archive full-text/OCR search without scraping or fabricating external baselines? Answered for v0: keep observations under `evals/search_usefulness/external_baselines/` with manual-only systems, JSON schema/template, operator instructions, a 64-query pending manifest across three baseline systems, and stdlib validation/report scripts. Pending slots are not observed baselines.
84. Which queries should make up the first manually observed external-baseline batch? Answered for Batch 0 preparation: select 13 high-value old-platform, member-discovery, driver/support-media, and article/scan query IDs under `evals/search_usefulness/external_baselines/batches/batch_0/`, covering all three manual-only baseline systems for 39 pending slots. The batch remains pending-only until a human operator records observations.
85. What local tooling should help a human fill Batch 0 without accidentally fabricating observations? Answered for v0: use `scripts/list_external_baseline_observations.py`, `scripts/create_external_baseline_observation.py`, `scripts/validate_external_baseline_observations.py --file`, and `scripts/report_external_baseline_status.py --batch batch_0 --next-pending` to list slots, create pending fillable JSON, validate one file, and summarize progress without scraping, URL fetching, browser automation, external querying, or observed-baseline claims.
86. What public-facing material is safe to prepare before any hosted live alpha? Answered for LIVE_ALPHA_00: keep a no-JS static public site pack under `site/dist/` with status, source matrix, eval/audit state, demo queries, limitations, roadmap, and local quickstart pages. Validate it with `scripts/validate_public_static_site.py`, and keep it documentation-only: no deployment, backend hosting, live probes, scraping, external baseline fabrication, or production-readiness claims.
87. What local evidence is needed before considering any public-alpha wrapper or deployment-config work? Answered for Public Alpha Rehearsal Evidence v0: collect static-site validation, public-alpha smoke, route inventory counts, eval/audit status, external-baseline pending status, blockers, next deployment requirements, and unsigned operator signoff under `docs/operations/public_alpha_rehearsal_evidence_v0/`, with `scripts/generate_public_alpha_rehearsal_evidence.py --check`. This is not deployment approval and adds no live probes.
88. What wrapper gate should sit between local public-alpha safe mode and deployment-config work? Answered for LIVE_ALPHA_01: use `scripts/run_public_alpha_server.py` plus `surfaces/web/server/public_alpha_config.py` as a stdlib public-alpha entrypoint/config guard. It defaults to localhost, requires `public_alpha`, refuses unsupported modes and unacknowledged nonlocal binds, reports safe capability flags, and keeps live probes, live Internet Archive access, local path controls, downloads/readback, user storage, deployment approval, and production readiness closed. It does not deploy Eureka or add auth/TLS/rate limiting/process management.
89. What contract layer should exist before GitHub Pages deployment or static generation? Answered for Public Publication Plane Contracts v0: use `control/inventory/publication/` plus `docs/architecture/PUBLICATION_PLANE.md`, reference docs, `scripts/validate_publication_inventory.py`, and publication inventory tests to govern public routes, route stability, public status vocabulary, client profiles, public data files, base-path portability, deployment target semantics, redirect policy, static artifact/source separation, and public claim traceability before any deployment or generator work.
90. Which public claim traceability rule should govern publication? Answered for v0: no public claim without a repo source. Allowed sources are README/status docs, source inventory, route inventory, eval/audit outputs, manual baseline records, static site manifest, and publication inventory.
91. How should GitHub Pages be enabled without letting hosting mechanics define Eureka's public architecture? Answered for GitHub Pages Deployment Enablement v0: add `.github/workflows/pages.yml`, `docs/operations/GITHUB_PAGES_DEPLOYMENT.md`, `scripts/check_github_pages_static_artifact.py`, and workflow/artifact tests so GitHub Actions validates the publication inventory and `site/dist/` before uploading only the static artifact. This is static-only, no backend, no live probes, no custom domain, no generator, no secrets, and no deployment-success claim without Actions evidence.
92. How should the static public site move toward generated output without replacing the deployed artifact too early? Answered for Static Site Generation Migration v0 and Repository Shape Consolidation v0: add a stdlib-only `site/` source tree with templates, page JSON, `site/build.py`, `site/validate.py`, and generated `site/dist/`, then make `site/dist/` the single generated GitHub Pages artifact. This adds no Node/npm, frontend framework, live backend behavior, live probes, external observations, or production claim.
93. What static public data should lite/text/files, snapshot, relay, and native clients consume before any live API exists? Answered for Generated Public Data Summaries v0: generate deterministic JSON summaries under `site/dist/data/` for site, page registry, source posture, eval/audit state, public-alpha route posture, and build provenance from governed repo inputs. These files are static summaries only; they add no live API, live probes, deployment behavior, external observations, or production JSON stability promise.
94. What compatibility surfaces should exist before snapshots, relay, native clients, or a live public API? Answered for Lite/Text/Files Seed Surfaces v0: generate static no-JS/no-download seed surfaces under `site/dist/lite/`, `site/dist/text/`, and `site/dist/files/` from the public data summaries, with file-tree manifests and SHA256SUMS. These surfaces are static publication artifacts only; they add no live search, executable downloads, signed snapshots, relay runtime, native-client runtime, live backend behavior, live probes, or production support claim.
95. Which resolver examples are safe to expose publicly before a live backend handoff exists? Answered for Static Resolver Demo Snapshots v0: generate static no-JS fixture-backed demo snapshots under `site/dist/demo/` for query planning, member-level results, compatibility evidence, absence, comparison, source detail, article/scan fixtures, and eval summaries. These demos are static publication examples only; they add no live search, live API semantics, backend hosting, external observations, production behavior, snapshots, relay runtime, or native-client runtime.
96. What must be true before considering a custom domain or alternate static host? Answered for Custom Domain / Alternate Host Readiness v0: add `domain_plan.json`, `static_hosting_targets.json`, base-path portability guidance, an unsigned future operator checklist, `scripts/validate_static_host_readiness.py`, and tests. This records prerequisites and validates no `CNAME`, DNS, provider config, alternate-host deployment, backend hosting, live probes, custom-domain claim, or production claim exists.
97. How should static publication surfaces hand off to a future live backend without implying that `/api/v1` exists today? Answered for Live Backend Handoff Contract v0: add `live_backend_handoff.json`, `live_backend_routes.json`, `surface_capabilities.json`, handoff/error-envelope reference docs, `scripts/validate_live_backend_handoff.py`, and tests. This reserves future `/api/v1` endpoint families and disabled live capability flags only; it adds no backend hosting, live route, live source probe, CORS/auth/rate-limit implementation, external observation, or production API guarantee.
98. What policy must exist before any live external source probe? Answered for Live Probe Gateway Contract v0: add `live_probe_gateway.json`, live probe gateway architecture/reference/policy docs, `scripts/validate_live_probe_gateway.py`, and tests. This defines disabled-by-default candidate source gates, caps, cache/evidence requirements, retry/circuit-breaker posture, no-download/no-arbitrary-URL-fetch rules, operator review, and Google as manual-baseline-only; it adds no probes, network calls, scraping, crawling, downloads, adapters, Internet Archive live access, or production source claim.
99. What should the first Rust query-planner parity candidate prove? Answered for v0: it should match expanded Python-oracle query-planner goldens for current old-platform cases as an isolated candidate only, with no Python runtime replacement, no web/CLI/API wiring, no live source behavior, and no production Rust claim.
100. How should Eureka coordinate modern web, old-browser, text, files, static data, future API, snapshots, relay, CLI, and native clients without pretending one surface is complete? Answered for Compatibility Surface Strategy v0: use `docs/architecture/COMPATIBILITY_SURFACES.md`, `surface_capabilities.json`, `surface_route_matrix.json`, old-client degradation policy, and native/snapshot/relay readiness docs to record same-resolver-truth/multiple-projection strategy. This adds no new runtime behavior, snapshots, relay services, native apps, live API, live probes, external observations, or production support claim.
101. What is the first snapshot format Eureka can define before relay or native clients exist? Answered for Signed Snapshot Format v0: define `control/inventory/publication/snapshot_contract.json`, `docs/reference/SNAPSHOT_FORMAT_CONTRACT.md`, `docs/reference/SNAPSHOT_SIGNATURE_POLICY.md`, `scripts/generate_static_snapshot.py`, `scripts/validate_static_snapshot.py`, and a tiny deterministic seed example under `snapshots/examples/static_snapshot_v0/`. This is a contract and seed example only: no real signing keys, production signatures, executable downloads, public `/snapshots/` route, relay runtime, native-client runtime, live backend, live probes, or production authenticity claim.

102. What local relay policy is needed before old, weak, offline, or protocol-limited clients can consume Eureka data through a trusted LAN bridge? Answered for Relay Surface Design v0: add `control/inventory/publication/relay_surface.json`, relay architecture/reference/security docs, an unsigned future operator checklist, `scripts/validate_relay_surface_design.py`, and tests. This is design and governance only: no relay runtime, sockets, FTP, SMB, WebDAV, AFP, NFS, Gopher, protocol proxy, private data exposure, write/admin routes, live-probe passthrough, native sidecar, or production relay claim.
103. After Relay Surface Design v0, should the next Codex-safe milestone be Rust Source Registry Parity Catch-up v0 or Signed Snapshot Consumer Contract v0? Default recommendation: Rust Source Registry Parity Catch-up v0, because it is non-network parity work and the source/capability shape has expanded since the first Rust source-registry candidate. Manual Observation Batch 0 remains human-operated parallel work.
104. What must be planned before a Rust Local Index candidate is implemented? Answered for Rust Local Index Parity Planning v0: add a planning-only parity plan, case map, acceptance-report schema, allowed/forbidden divergence policy, stdlib validator, and tests. Future Rust must match Python local-index source records, resolved objects, representations, members, synthetic members, evidence, article/scan records, result lanes, user-cost, compatibility evidence, parent/member lineage, deterministic ordering, and FTS/fallback normalization. This adds no Rust local-index implementation, SQLite/indexing behavior, Python runtime replacement, or runtime/surface wiring.
105. How should future clients consume signed/offline snapshots before any relay or native runtime exists? Answered for Signed Snapshot Consumer Contract v0: add `snapshot_consumer_contract.json`, `snapshot_consumer_profiles.json`, `docs/reference/SNAPSHOT_CONSUMER_CONTRACT.md`, `scripts/validate_snapshot_consumer_contract.py`, and tests. The contract defines read order, checksum validation, v0 signature-placeholder semantics, missing optional file behavior, unsupported feature handling, and future file-tree/text/lite/relay/native/audit consumer profiles. This adds no snapshot reader runtime, relay runtime, native client, production signing, real signing keys, executable downloads, live backend, or live probes.
106. What native-client boundary should exist before any Visual Studio, Xcode, GUI, FFI, installer, or download work? Answered for Native Client Contract v0: add `native_client_contract.json`, `native_client_lanes.json`, `docs/reference/NATIVE_CLIENT_CONTRACT.md`, `docs/reference/NATIVE_CLIENT_LANES.md`, `docs/operations/NATIVE_CLIENT_READINESS_CHECKLIST.md`, `scripts/validate_native_client_contract.py`, and tests. The contract defines future Windows/macOS lanes, CLI current-state honesty, allowed static/snapshot/API/relay inputs, required verification, Rust/Python boundaries, and prohibited install/download/action behavior. This adds no native app project, native GUI, FFI, native snapshot reader runtime, relay sidecar, installer automation, package-manager behavior, executable download/execution automation, live probes, or Rust runtime wiring.
107. What policy must exist before native clients or public surfaces offer downloads, mirrors, install handoff, package-manager handoff, execution, restore, uninstall, rollback, malware scanning, or rights/access claims? Answered for Native Action / Download / Install Policy v0: add `action_policy.json`, action/download/install, executable-risk, rights/access, and install-handoff reference docs, `scripts/validate_action_policy.py`, and tests. The policy classifies safe read-only actions, bounded fixture/local actions, future gated risky actions, prohibited silent/privileged/private actions, warning classes, and static/public-alpha defaults. This adds no downloads, installers, package-manager integration, malware scanning, rights clearance, native clients, relay runtime, executable trust claims, or public download surface.
108. What privacy policy must exist before native clients add local cache, private data, local path, telemetry/logging, diagnostics, accounts, cloud sync, or portable/offline behavior? Answered for Native Local Cache / Privacy Policy v0: add `local_cache_privacy_policy.json`, local cache/privacy, native cache, and telemetry/logging reference docs, `scripts/validate_local_cache_privacy_policy.py`, and tests. The policy keeps private cache, private ingestion, telemetry, analytics, accounts, cloud sync, uploads, old-client private relay, and local archive scanning disabled by default. This adds no cache runtime, private file ingestion, telemetry implementation, account system, cloud sync, upload path, native client, relay runtime, or private-data relay behavior.
109. Is Eureka ready to create native client project scaffolds? Answered for Native Client Project Readiness Review v0: add `control/audits/native-client-project-readiness-v0/`, `native_readiness_report.json`, `scripts/validate_native_project_readiness_review.py`, and tests. The decision is `ready_for_minimal_project_skeleton_after_human_approval` for `windows_7_x64_winforms_net48` only. This is review/evidence only and adds no Visual Studio/Xcode projects, native app source tree, GUI behavior, FFI, cache runtime, downloads, installers, relay runtime, live probes, or runtime wiring. The next safe step was Windows 7 WinForms Native Skeleton Planning v0, not implementation.
110. What exactly may the first Windows native skeleton be after approval? Answered for Windows 7 WinForms Native Skeleton Planning v0: add `control/audits/windows-7-winforms-native-skeleton-planning-v0/`, `winforms_skeleton_plan.json`, `scripts/validate_windows_winforms_skeleton_plan.py`, and tests. The plan proposes `clients/windows/winforms-net48/` and `Eureka.Clients.Windows.WinForms`, requires a Windows host with Visual Studio 2022, .NET Framework 4.8 targeting/developer pack, x64 target, and Windows 7 SP1+ runtime verification, and limits future scope to read-only static public data and seed snapshot demo inspection. It adds no `clients/`, Visual Studio solution, `.csproj`, C# source, GUI behavior, FFI, cache runtime, downloads, installers, telemetry, relay runtime, live probes, or runtime wiring. Any implementation requires explicit human approval.
111. What is the smallest safe first relay prototype? Answered for Relay Prototype Planning v0: add `control/audits/relay-prototype-planning-v0/`, `relay_prototype_plan.json`, `scripts/validate_relay_prototype_plan.py`, and tests. The plan selects `local_static_http_relay_prototype` as the first future candidate with localhost-only, read-only, static public-data/seed-snapshot scope. It allows only allowlisted public data, text/files seed surfaces, and seed snapshot files, and prohibits arbitrary user directories, credentials, live API responses, live probe outputs, external URLs, write/upload/admin endpoints, arbitrary file serving, private paths, executable launch, telemetry, and download/install behavior. It adds no relay server, sockets, local HTTP relay, protocol server, native sidecar, snapshot mount, private file serving, live backend proxy, live probe, or old-client relay support. Any implementation requires explicit human approval.
112. What should the next safe project-wide checkpoint recommend after relay/native planning? Answered for Full Project State Audit v0: create `control/audits/full-project-state-audit-v0/`, `full_project_audit_report.json`, `scripts/validate_full_project_state_audit.py`, and tests. The audit records current milestone state, broad verification, 0 observed / 192 pending external-baseline slots, Cargo unavailable status, risks, blockers, human-operated work, explicit deferrals, and recommends Public Data Contract Stability Review v0 next with Generated Artifact Drift Guard v0 as the alternative. It adds no product behavior.
113. Which generated public data fields may future static, snapshot, relay, and native clients safely consume before Eureka has a production API? Answered for Public Data Contract Stability Review v0: add `control/audits/public-data-contract-stability-review-v0/`, `public_data_stability_report.json`, `docs/reference/PUBLIC_DATA_STABILITY_POLICY.md`, `scripts/validate_public_data_stability.py`, and tests. The review keeps public JSON pre-alpha and not production-stable, classifies field paths as `stable_draft`, `experimental`, `volatile`, `internal`, `deprecated`, or `future`, and recommends Generated Artifact Drift Guard v0 next. It adds no runtime behavior, live API, deployment behavior, relay/native/snapshot-reader implementation, or production API claim.
114. How do we keep committed generated and generated-like artifacts synchronized with their owning contracts and generators? Answered for Generated Artifact Drift Guard v0: add `control/inventory/generated_artifacts/`, `docs/operations/GENERATED_ARTIFACT_DRIFT_GUARD.md`, `control/audits/generated-artifact-drift-guard-v0/`, `scripts/check_generated_artifact_drift.py`, and tests. The guard records artifact ownership and runs deterministic non-mutating checks for public data, lite/text/files surfaces, static demos, seed snapshots, `site/dist`, Python oracle goldens, public-alpha rehearsal evidence, publication inventories, test registry metadata, and AIDE metadata. It does not regenerate by default, call external services, deploy, open sockets, or change runtime behavior.
115. How should the repository layout consolidate before public search, static publication review, source packs, snapshots, native clients, relay, or hosted services build on top of it? Answered for Repository Shape Consolidation v0: make `site/dist/` the single generated static deployment artifact, remove the active legacy static artifact path, configure the Pages workflow and artifact checker for `site/dist`, record `static_site_dist` in generated artifact inventory, add a repository layout validator and tests, and confirm `external/` as the outside-reference root. This adds no public search runtime, API routes, live backend hosting, live probes, crawling, external search automation, relay runtime, native client, downloads, accounts, telemetry, auth, TLS, rate limiting, production signing, or production readiness claim.
116. Is `site/dist/` ready to be treated as Eureka's active static publication artifact? Answered for Static Artifact Promotion Review v0: conditionally promote `site/dist/` as the active repo-local static artifact, record the audit pack under `control/audits/static-artifact-promotion-review-v0/`, validate workflow/generated-artifact/static-safety/base-path/public-data/stale-reference evidence, and keep GitHub Actions deployment status unverified until a run-evidence review records it. This adds no public search runtime, backend hosting, live probes, crawling, external search automation, relay runtime, native client, downloads, accounts, telemetry, auth, TLS, rate limiting, production signing, or production readiness claim.
117. What did the first GitHub Pages run-evidence review prove after `site/dist` promotion? Answered for GitHub Pages Run Evidence Review v0: the workflow is configured and validates `site/dist`, but the current-head run failed at Pages configuration because the repository Pages site was not found/enabled for GitHub Actions. No Pages artifact was uploaded and no deployment URL was emitted. The next Pages milestone is GitHub Pages Workflow Repair v0; this review does not deploy anything, add backend hosting, add live search, or claim production readiness.
118. What public search contract should exist before any hosted runtime route is added? Answered for Public Search API Contract v0: define contract-only request, response, error, and route envelopes for future `local_index_only` public search under `contracts/api/`, reserve `/search` and `/api/v1/search` as future routes, require bounded query/result limits and stable error codes, and explicitly forbid live probes, arbitrary URL fetch, local path parameters, downloads, installs, uploads, credentials, crawling, scraping, backend hosting, production API stability, and any claim that public search is live.
119. What result-card contract should exist before public search displays or API responses emit results? Answered for Public Search Result Card Contract v0: define `contracts/api/search_result_card.v0.json`, fixture-safe examples, an audit pack, reference docs, validator, and tests for future web/API/lite/text/native/relay/snapshot/contribution consumers. The card exposes lanes, user-cost, source and public identity, evidence, compatibility, member/representation context, actions, rights, risk, warnings, limitations, and gaps while blocking or future-gating downloads, installers, execution, uploads, mirrors, restore/rollback, and private-source submission. This adds no runtime search, live routes, live probes, backend hosting, malware-safety claim, rights-clearance claim, production ranking guarantee, or production API claim.
120. What safety and abuse guardrails must exist before public search runtime work? Answered for Public Search Safety / Abuse Guard v0: define `control/inventory/publication/public_search_safety.json`, `docs/operations/PUBLIC_SEARCH_SAFETY_AND_ABUSE_GUARD.md`, `docs/operations/PUBLIC_SEARCH_RUNTIME_READINESS_CHECKLIST.md`, a stdlib validator, and tests. The guard fixes `local_index_only` as the only v0 mode, bounds query/result/time behavior, forbids URL/local path/credential/download/install/upload parameters, maps disallowed behavior to the P26 error envelope, keeps telemetry and raw query logging off by default, and lists future operator controls. This adds no runtime search, live routes, rate-limit middleware, telemetry runtime, auth/accounts, hosted backend, live probes, downloads, uploads, arbitrary URL fetch, local path search, or production safety claim.
121. How should the first public search runtime behave without overreaching into hosted search? Answered for Local Public Search Runtime v0: add a gateway-owned `local_index_only` public search service, stdlib web/API routes for `/search`, `/api/v1/search`, `/api/v1/query-plan`, `/api/v1/status`, `/api/v1/sources`, and `/api/v1/source/{source_id}`, server-rendered no-JS HTML, governed JSON/error/result-card envelopes, public-alpha smoke coverage, and validators/tests. The runtime uses repo-owned/demo catalog behavior and rejects forbidden URL/local-path/credential/download/install/upload/live-probe parameters. It adds no hosted deployment, live probes, external calls, arbitrary URL fetch, downloads, installers, uploads, accounts, telemetry, rate-limit middleware, native clients, relay runtime, production API stability, or production readiness claim.
122. How should the static site hand users to local public search without pretending hosted search exists? Answered for Public Search Static Handoff v0: add `control/inventory/publication/public_search_handoff.json`, `site/dist/search.html`, `site/dist/lite/search.html`, `site/dist/text/search.txt`, `site/dist/files/search.README.txt`, `site/dist/data/search_handoff.json`, `scripts/validate_public_search_static_handoff.py`, and tests. The handoff is no-JS and static-only, keeps hosted backend status unavailable/unverified, disables the hosted form, publishes local/prototype runtime instructions, and adds no backend hosting, live probes, downloads, installs, uploads, local path search, telemetry, fake hosted URL, or production claim.
123. How should the local public search stack be rehearsed before source expansion or hosted planning? Answered for Public Search Rehearsal v0: add `control/audits/public-search-rehearsal-v0/`, expand `scripts/public_search_smoke.py`, add `scripts/validate_public_search_rehearsal.py`, and add script/operations/hardening tests. The rehearsal records route coverage, representative safe query outcomes, governed blocked request outcomes, static handoff review, public-alpha review, and contract alignment without deploying hosted search, calling external sources, enabling live probes, downloads, installs, uploads, local path search, accounts, telemetry, or production claims.
124. Which fixture-only source families should reduce the next broad search-usefulness gaps without live calls? Answered for Search Usefulness Source Expansion v2: add `runtime/connectors/source_expansion_recorded/`, six recorded fixture-only source inventory records, 15 tiny metadata fixture records, local catalog/index/public-search integration, `control/audits/search-usefulness-source-expansion-v2/`, `scripts/validate_source_expansion_v2.py`, and focused connector/index/public-search/eval/operations tests. The broad audit moved from `covered=5`, `partial=22`, `source_gap=26`, `capability_gap=9`, `unknown=2` to `covered=5`, `partial=40`, `source_gap=10`, `capability_gap=7`, `unknown=2` while keeping all external baselines pending/manual and adding no live probes, scraping, crawling, URL fetching, external observations, real binaries, downloads, uploads, local path search, hosted search, or production relevance claim.
125. What did Source Expansion v2 change, and what should follow? Answered for Search Usefulness Delta v2: add `control/audits/search-usefulness-delta-v2/`, `scripts/validate_search_usefulness_delta_v2.py`, and focused script/operations tests. The delta uses the committed P32 report as the machine-derived baseline, confirms current counts of `covered=5`, `partial=40`, `source_gap=10`, `capability_gap=7`, `unknown=2`, records selected `source_gap_to_partial` movements, current failure-mode counts, source-family impact, public-search smoke status, hard-eval status, external-baseline pending status, and remaining gaps. Exact failure-mode deltas are marked unavailable because the pre-P32 failure-mode baseline was not committed as machine-readable JSON. The next milestone was Source Pack Contract v0, now implemented.
126. What is a safe user-extension path before import, upload, or master-index behavior exists? Answered for Source Pack Contract v0: add `contracts/packs/source_pack.v0.json`, `docs/reference/SOURCE_PACK_CONTRACT.md`, `docs/reference/PACK_LIFECYCLE.md`, `examples/source_packs/minimal_recorded_source_pack_v0/`, `scripts/validate_source_pack.py`, and `control/audits/source-pack-contract-v0/`. Source packs are portable source metadata and fixture-evidence bundles with rights/privacy docs, checksums, disabled live/network behavior, and prohibited import/upload/executable/master-index behavior. This adds no runtime import, indexing, upload, live connector, arbitrary URL fetch, executable plugin, download/install surface, master-index acceptance, rights-clearance claim, malware-safety claim, or production extension support. The next milestone is Evidence Pack Contract v0.
127. How can users share claims and observations without sharing full source packs, private caches, local indexes, or executable payloads? Answered for Evidence Pack Contract v0: add `contracts/packs/evidence_pack.v0.json`, `docs/reference/EVIDENCE_PACK_CONTRACT.md`, `examples/evidence_packs/minimal_evidence_pack_v0/`, `scripts/validate_evidence_pack.py`, and `control/audits/evidence-pack-contract-v0/`. Evidence packs are portable public-safe claim/observation bundles with source locators, snippet limits, rights/privacy docs, checksums, disabled live/network behavior, and prohibited import/upload/executable/master-index behavior. This adds no runtime import, indexing, upload, live connector, arbitrary URL fetch, executable plugin, download/install surface, master-index acceptance, canonical truth selection, rights-clearance claim, malware-safety claim, or production extension support. Index Pack Contract v0 is now implemented.
128. How can users share index coverage and record summaries without sharing private caches, raw SQLite databases, raw source payloads, or executable artifacts? Answered for Index Pack Contract v0: add `contracts/packs/index_pack.v0.json`, `docs/reference/INDEX_PACK_CONTRACT.md`, `examples/index_packs/minimal_index_pack_v0/`, `scripts/validate_index_pack.py`, and `control/audits/index-pack-contract-v0/`. Index packs are portable public-safe summary-only bundles with index build metadata, source coverage, field coverage, query examples, record summaries, privacy/rights docs, checksums, disabled live/network behavior, and prohibited import/merge/upload/raw-database/executable/master-index behavior. This adds no runtime import, merge, indexing, upload, raw SQLite export, local cache export, live connector, arbitrary URL fetch, executable plugin, download/install surface, master-index acceptance, canonical truth selection, rights-clearance claim, malware-safety claim, or production extension support. Contribution Pack Contract v0 is now implemented.
129. How can users wrap source, evidence, index, manual observation, correction, alias, compatibility, absence, and result-feedback candidates for later review without upload/import behavior? Answered for Contribution Pack Contract v0: add `contracts/packs/contribution_pack.v0.json`, `docs/reference/CONTRIBUTION_PACK_CONTRACT.md`, `examples/contribution_packs/minimal_contribution_pack_v0/`, `scripts/validate_contribution_pack.py`, and `control/audits/contribution-pack-contract-v0/`. Contribution packs are portable review-candidate bundles with referenced pack records, contribution item types, proposed actions, pending manual-observation placeholders, rights/privacy docs, checksums, disabled live/network behavior, and prohibited upload/import/raw-cache/executable/master-index behavior. This adds no contribution upload, source/evidence/index pack import, moderation UI, accounts, identity, master-index review queue runtime, automatic acceptance, live connector, arbitrary URL fetch, executable plugin, download/install surface, master-index acceptance, canonical truth selection, rights-clearance claim, malware-safety claim, or production extension support. Master Index Review Queue Contract v0 is now implemented.
130. How can a future hosted/master Eureka index review candidate contributions without blindly trusting arbitrary packs, caches, AI output, live-probe results, or executable payloads? Answered for Master Index Review Queue Contract v0: add `contracts/master_index/review_queue_manifest.v0.json`, `contracts/master_index/review_queue_entry.v0.json`, `contracts/master_index/review_decision.v0.json`, `control/inventory/master_index/`, `docs/reference/MASTER_INDEX_REVIEW_QUEUE_CONTRACT.md`, `docs/architecture/MASTER_INDEX_REVIEW_QUEUE.md`, `examples/master_index_review_queue/minimal_review_queue_v0/`, `scripts/validate_master_index_review_queue.py`, and `control/audits/master-index-review-queue-contract-v0/`. The queue contract defines review entries, decisions, validation and review states, privacy/rights/risk classification, conflict preservation, limited accepted_public semantics, and acceptance requirements. This adds no queue runtime, upload handling, contribution import, source/evidence/index pack import, moderation UI, accounts, hosted master index, master-index writes, automatic acceptance, live connector, external observation, arbitrary URL fetch, executable plugin, download/install surface, rights-clearance claim, malware-safety claim, canonical truth claim, or production extension support. Source/Evidence/Index Pack Import Planning v0 is now implemented.
131. How should source, evidence, index, and contribution packs be imported later without blindly trusting user data or mutating search/master-index state? Answered for Source/Evidence/Index Pack Import Planning v0: add `control/audits/pack-import-planning-v0/`, `docs/reference/PACK_IMPORT_PLANNING.md`, `docs/architecture/PACK_IMPORT_PIPELINE.md`, `scripts/validate_pack_import_planning.py`, and focused script/operations tests. The planning pack defines import scope, validate-only first mode, stage-local-quarantine as the next mode, validation pipeline, private staging rules, privacy/rights/risk review, provenance and claim handling, local search/index impact, native/snapshot/relay impact, and master-index review interaction. This adds no import runtime, staging directories, arbitrary directory scan, live fetch, local search mutation, local index mutation, canonical source registry mutation, upload, hosted/master-index mutation, automatic acceptance, executable plugin behavior, rights-clearance claim, malware-safety claim, canonical truth claim, or production extension support. Pack Import Validator Aggregator v0 is now implemented.
132. Can all known pack and review-queue examples be validated through one safe command before import exists? Answered for Pack Import Validator Aggregator v0: add `scripts/validate_pack_set.py`, `control/inventory/packs/example_packs.json`, `docs/operations/PACK_VALIDATION.md`, `control/audits/pack-import-validator-aggregator-v0/`, and focused script/operations tests. The aggregate validator lists known examples, validates all five current examples, validates one explicit pack root, detects pack type by manifest, reports passed/failed/unavailable/unknown_type, emits JSON with all side-effect flags false, and delegates to existing validators without weakening their checks. This adds no import runtime, staging directory, local index mutation, upload, submission, moderation, account, hosted/master-index mutation, automatic acceptance, live fetch, executable plugin behavior, rights-clearance claim, malware-safety claim, canonical truth claim, or production extension support. The next milestone is AI Provider Contract v0.
133. How should AI, local model, and remote model providers be represented before AI runtime exists? Answered for AI Provider Contract v0: add `contracts/ai/`, `control/inventory/ai_providers/`, `examples/ai_providers/disabled_stub_provider_v0/`, `docs/reference/AI_PROVIDER_CONTRACT.md`, `docs/reference/TYPED_AI_OUTPUT_CONTRACT.md`, `docs/architecture/AI_ASSISTANCE_BOUNDARY.md`, `scripts/validate_ai_provider_contract.py`, `control/audits/ai-provider-contract-v0/`, and focused script/operations tests. The contract defines disabled-by-default provider manifests, future task requests, typed output envelopes, allowed suggestion tasks, forbidden authority tasks, privacy/credential/logging/cache policy, evidence-linking policy, and review requirements. This adds no model calls, API keys, credential storage, telemetry, prompt logging runtime, embeddings, vector search, LLM reranking, AI in public search, AI-generated evidence acceptance, local index mutation, hosted/master-index mutation, truth authority, rights-clearance claim, malware-safety claim, automatic acceptance, or production AI support. The next milestone is Typed AI Output Validator v0.
134. How should standalone typed AI output records be checked before any AI-assisted evidence or contribution workflow? Answered for Typed AI Output Validator v0: add `runtime/engine/ai/typed_output_validator.py`, `scripts/validate_ai_output.py`, `control/inventory/ai_providers/typed_output_examples.json`, four disabled-stub valid output examples, `docs/operations/AI_OUTPUT_VALIDATION.md`, `control/audits/typed-ai-output-validator-v0/`, and focused runtime/script/operations tests. The validator enforces allowed task/output types, provider reference alignment, disabled-by-default provider posture, required review, prohibited canonical-truth/rights-clearance/malware-safety/automatic-acceptance uses, private-path and secret rejection, generated-text size limits, and no model/network/mutation/import side effects. This adds no model calls, provider runtime, API keys, telemetry, evidence import, contribution import, local index mutation, public-search AI, upload, hosted/master-index mutation, truth authority, rights-clearance claim, malware-safety claim, or production AI support. The next milestone was Pack Import Report Format v0, now implemented.
135. What durable report should future validate-only pack import tooling emit before any staging, indexing, submission, or merge can occur? Answered for Pack Import Report Format v0: add `contracts/packs/pack_import_report.v0.json`, `examples/import_reports/`, `scripts/validate_pack_import_report.py`, `docs/reference/PACK_IMPORT_REPORT_FORMAT.md`, `control/audits/pack-import-report-format-v0/`, and focused script/operations tests. The report format records pack validation results, unknown-type and private-path failures, issue summaries, privacy/rights/risk posture, provenance, next actions, and hard false mutation-safety fields. This adds no import runtime, staging directories, local index mutation, runtime source registry mutation, public-search mutation, upload, submission, model calls, network calls, hosted/master-index mutation, truth authority, rights-clearance claim, malware-safety claim, or production support. Validate-Only Pack Import Tool v0 is now implemented to emit that report format without staging or importing packs.
136. Can Eureka run a validate-only pack import preflight without treating validation as import or acceptance? Answered for Validate-Only Pack Import Tool v0: add `scripts/validate_only_pack_import.py`, `docs/operations/VALIDATE_ONLY_PACK_IMPORT.md`, `control/audits/validate-only-pack-import-tool-v0/`, focused script/operations tests, and AIDE/test metadata. The tool validates explicit pack roots or known examples, optionally includes typed AI output examples, delegates to existing validators, emits Pack Import Report v0, and writes only an explicit report file when `--output` is provided. It does not import, stage, index, upload, mutate runtime state, mutate public search, call networks, call models, mutate the master index, prove truth, prove rights clearance, or prove malware safety. The next milestone is Manual Observation Batch 0 Execution, human-operated; the Codex-safe alternative is Local Quarantine/Staging Model v0 as planning only.
137. How should future local quarantine/staging be modeled before staging runtime exists? Answered for Local Quarantine/Staging Model v0: add `control/inventory/local_state/`, `.gitignore` local-state protections, `docs/architecture/LOCAL_QUARANTINE_STAGING_MODEL.md`, `docs/reference/LOCAL_STAGING_PATH_POLICY.md`, `scripts/validate_local_quarantine_staging_model.py`, focused tests, and `control/audits/local-quarantine-staging-model-v0/`. The model defines local_private defaults, future private roots, prohibited roots, staged entity vocabulary, validate-only report linking, privacy/rights/risk posture, reset/delete/export requirements, and no-impact search/master-index defaults. It creates no staging runtime, no `.eureka-local/` state, no staged pack contents, no import, no local index mutation, no public-search mutation, no upload, no model or network calls, no truth/rights/malware claims, and no master-index mutation. The next Codex-safe milestone is Staging Report Path Contract v0; Local Staging Manifest Format v0 is the alternative.
138. Where may future staging/import reports be written before local staging manifests exist? Answered for Staging Report Path Contract v0: add `control/inventory/local_state/staging_report_path_contract.json`, `.eureka-reports/` ignore protection, `docs/reference/STAGING_REPORT_PATH_CONTRACT.md`, `docs/operations/LOCAL_REPORT_PATHS.md`, `scripts/validate_staging_report_path_contract.py`, focused tests, validate-only output-root enforcement, and `control/audits/staging-report-path-contract-v0/`. The contract keeps report output on stdout by default, requires explicit output paths for file writes, blocks forbidden public/runtime/canonical roots, defines future local/private report roots, requires redaction of private local paths, and creates no report path runtime, staging runtime, staged state, import, local index mutation, public-search mutation, upload, network/model calls, rights/malware/truth claims, or master-index mutation. The next Codex-safe milestone is Local Staging Manifest Format v0; Staged Pack Inspector v0 follows.
139. What local staging manifest envelope should exist before staged inspection or staging runtime? Answered for Local Staging Manifest Format v0: add `contracts/packs/local_staging_manifest.v0.json`, `examples/local_staging_manifests/minimal_local_staging_manifest_v0/`, `docs/reference/LOCAL_STAGING_MANIFEST_FORMAT.md`, `scripts/validate_local_staging_manifest.py`, focused script/operations tests, local-state inventory references, and `control/audits/local-staging-manifest-format-v0/`. The format records validate-only report refs, staged pack refs, staged entity candidates, counts, privacy/rights/risk posture, provenance, hard no-mutation guarantees, and reset/delete/export policy. This adds no staging runtime, no `.eureka-local/` state, no staged pack contents, no import, no local index mutation, no public-search mutation, no runtime source registry mutation, no upload, no network/model calls, no truth/rights/malware claims, and no master-index mutation. The next Codex-safe milestone is Staged Pack Inspector v0; Manual Observation Batch 0 Execution remains human-operated parallel work.
140. Can Eureka inspect a local staging manifest without staging anything? Answered for Staged Pack Inspector v0: add `scripts/inspect_staged_pack.py`, `scripts/validate_staged_pack_inspector.py`, `docs/operations/STAGED_PACK_INSPECTION.md`, focused script/operations tests, local-state inventory references, and `control/audits/staged-pack-inspector-v0/`. The inspector reads explicit Local Staging Manifest v0 files/roots or committed synthetic examples, validates manifests before inspection by default, emits human and JSON summaries, redacts obvious private paths/secrets, and reports staged pack refs, candidate entity counts, privacy/rights/risk summaries, reset/delete/export policy, and hard no-mutation guarantees. This adds no staging runtime, no `.eureka-local/` state, no staged pack contents, no import, no local index mutation, no public-search mutation, no runtime source registry mutation, no upload, no network/model calls, no truth/rights/malware claims, and no master-index mutation. The immediate next milestone is Manual Observation Batch 0 Execution, human-operated; AI-Assisted Evidence Drafting Plan v0 is the Codex-safe alternative.
141. How may optional future AI providers assist evidence or contribution drafting without becoming a truth layer? Answered for AI-Assisted Evidence Drafting Plan v0: add `control/inventory/ai_providers/ai_assisted_drafting_policy.json`, `docs/architecture/AI_ASSISTED_EVIDENCE_DRAFTING.md`, `docs/reference/AI_ASSISTED_DRAFTING_CONTRACT.md`, synthetic examples under `examples/ai_assisted_drafting/minimal_drafting_flow_v0/`, `scripts/validate_ai_assisted_drafting_plan.py`, focused tests, and `control/audits/ai-assisted-evidence-drafting-plan-v0/`. AI-assisted drafting is planning/example/validation only: remote providers remain disabled by default, private data is not allowed by default, typed output validation and required review gate every future candidate, and outputs may map only to evidence/contribution/master-index review candidates later. This adds no AI runtime, model calls, provider loading, API keys, telemetry, evidence/contribution acceptance, public-search mutation, local-index mutation, rights-clearance claim, malware-safety claim, canonical truth claim, or master-index mutation. The immediate next milestone is Manual Observation Batch 0 Execution, human-operated; Public Hosted Search Rehearsal Plan v0 is the Codex-safe alternative.
142. What should P50 conclude after the public search, source, pack, staging, and AI queue? Answered for Post-P49 Platform Audit v0: static publication and local/prototype search are validated, source coverage remains fixture/recorded-fixture bounded, external baselines remain manual-pending, pack import/staging are validate-only/planning/read-only, AI is candidate-only planning/validation, hosted backend and live connectors are gated, and the largest Codex-side gap is query intelligence. The next branch is `p51-post-p50-remediation-pack-v0`.
143. Which root governance documents are enough before public-hosted behavior exists? Answered for Post-P50 Remediation Pack v0: add minimal root `CONTRIBUTING.md`, `SECURITY.md`, and `CODE_OF_CONDUCT.md` documents that preserve auditability, forbid unapproved live/source/model/secret/product behavior, and mark private security/conduct contact as pending. These are pre-production placeholders, not complete legal, privacy, takedown, incident-response, or hosted-service policies.
144. Which license should Eureka use? Pending human/legal decision. P51 adds `docs/operations/LICENSE_SELECTION_REQUIRED.md` and deliberately does not add a root `LICENSE` file because README says licensing is not finalized and no authoritative repo decision selects an open-source license.
145. What does P52 prove about GitHub Pages deployment? Answered for Static Deployment Evidence / GitHub Pages Repair v0: the workflow is configured for `site/dist` and local static artifact validation passes, but `gh` is unavailable in this environment, current-head Actions/Pages status is unverified, prior committed evidence records a Pages configuration failure before artifact upload, and deployment remains operator-gated until a human enables Pages GitHub Actions source, reruns the workflow, and records the deployed URL/run evidence. P52 adds no hosted backend, public search hosting, live probes, credentials, telemetry, uploads, downloads, accounts, or production readiness claim.
146. What contract must P54 implement before any hosted public-search wrapper can be exposed? Answered for Public Search Production Contract v0: preserve `local_index_only` as the only active mode, enforce a 160-character query limit and 10/25 default/max result limits, serve only the documented GET routes, reject forbidden local path, URL fetch, credential, live-probe, download, install, execute, upload, and arbitrary-source parameters, return public-safe error envelopes, emit aligned result-card/source-status/evidence-summary/absence/status shapes, and keep hosted search, dynamic backend deployment, live probes, downloads, uploads, installs, local paths, arbitrary URL fetch, accounts, telemetry, AI runtime, and index mutation disabled until later approved milestones.
147. What should the first hosted public-search wrapper do before deployment evidence exists? Answered for Hosted Public Search Wrapper v0: add `scripts/run_hosted_public_search.py`, `scripts/check_hosted_public_search_wrapper.py`, `scripts/validate_hosted_public_search_wrapper.py`, hosted-operation docs, inert Docker/Render templates, metadata, tests, and `control/audits/hosted-public-search-wrapper-v0/`. The wrapper serves `/healthz`, `/status`, `/search`, and the P53 `/api/v1` public-search routes in `local_index_only` mode only, rejects forbidden public parameters, and keeps hosted deployment unverified with no live probes, downloads, uploads, install actions, local paths, arbitrary URL fetch, accounts, telemetry, external calls, AI runtime, source connector runtime, index mutation, pack import, or staging runtime. The next Codex-safe milestone is P55 Public Search Index Builder v0; actual hosting evidence is operator-gated.
148. What controlled public index should the hosted/local public-search path use before live sources exist? Answered for Public Search Index Builder v0: add `scripts/build_public_search_index.py`, `scripts/validate_public_search_index.py`, `scripts/validate_public_search_index_builder.py`, committed `data/public_index` manifests/coverage/stats/checksums/NDJSON, runtime loading from that generated bundle, docs, tests, drift metadata, and `control/audits/public-search-index-builder-v0/`. The index contains 584 public-safe documents derived from governed source inventory plus committed fixture/recorded metadata only, uses deterministic lexical fallback search, and adds no live probes, external source calls, private path ingestion, executable payloads, downloads, uploads, arbitrary URL fetching, telemetry, AI runtime, pack import, staging runtime, master-index mutation, hosted deployment, or production search-quality claim. The next Codex-safe milestone is P56 Static Site Search Integration v0.
149. What local safety evidence should exist before hosted public-search rehearsal? Answered for Public Search Safety Evidence v0: add `scripts/run_public_search_safety_evidence.py`, `scripts/validate_public_search_safety_evidence.py`, `control/audits/public-search-safety-evidence-v0/`, docs, tests, and metadata. The evidence covers safe queries, 32 blocked request cases, limits, status endpoints, static handoff safety, public index safety, hosted-wrapper local rehearsal, privacy/redaction, and operator-gated rate-limit/edge status without deployment, live probes, external calls, downloads, uploads, installers, accounts, telemetry, arbitrary URL fetching, AI runtime, index mutation, or production claim.
149. How should the static site connect users to search before a hosted backend URL exists? Answered for Static Site Search Integration v0: keep the default backend status `backend_unconfigured`, generate no-JS `search.html`, `lite/search.html`, `text/search.txt`, and `files/search.README.txt`, publish `data/search_config.json` and `data/public_index_summary.json`, show local wrapper instructions and sample queries, and disable hosted form submission until operator evidence records a verified backend URL. This adds no hosted deployment, fake URL, live probes, external source calls, downloads, uploads, accounts, telemetry, arbitrary URL fetching, AI runtime, index mutation, pack import, staging runtime, or production search-quality claim.
150. Answered by P58: the hosted wrapper can start locally under hosted-safe settings and pass route, safe-query, blocked-request, static handoff, public-index, and template checks. Still open/operator-gated: real hosted deployment evidence, backend URL configuration, edge/rate limits, and production readiness.


## P59 Query Intelligence Open Questions

P60 answer: the shared query/result cache is contract-only. It may later reuse
safe result summaries and scoped absence outcomes, but current runtime does not
write cache entries, persist cache state, publish query cache entries, mutate a
miss ledger/search need/probe queue/candidate index, or mutate the master index.
The next question is how P61 should classify and validate misses as ledger
entries without treating them as truth.

P61 answer: the search miss ledger is contract-only. It classifies no-hit,
weak-hit, near-miss, blocked, and incomplete searches with checked/not-checked
scope, cause records, privacy flags, scoped absence, and future-only suggested
steps. Current runtime does not write miss ledger entries, create search needs,
enqueue probes, mutate result caches, mutate candidate indexes, or mutate the
master index. The next question is how P62 should represent durable search
needs without turning misses into truth.

P62 answer: search need records are contract-only scoped unresolved needs. They
can later aggregate privacy-filtered observations, cache summaries, and miss
ledger refs, but P62 adds no runtime need store, public demand counts, probes,
candidate index, or master-index mutation. The next question is how P63 should
define a probe queue without enabling live probes.

P63 answer: probe queue items are contract-only future work requests. They may
later point from search needs toward manual observation, source-cache work,
deep extraction, or approval-gated source metadata checks, but P63 adds no
runtime queue, no probe execution, no live source calls, no source-cache or
evidence-ledger mutation, no candidate-index mutation, and no master-index
mutation. The next question is how P64 should model candidate records without
turning probe plans into truth.

P64 answer: candidate index records are contract-only provisional review
records. They can later represent possible objects, evidence, compatibility,
absence, conflicts, source matches, and identity candidates, but P64 adds no
runtime candidate index, no public search candidate injection, no candidate
promotion runtime, no source-cache or evidence-ledger mutation, and no
master-index mutation. The next question is how P65 should define promotion
policy without accepting candidates as truth too early.

- What aggregate threshold is required before public demand summaries may be published?
- What retention and deletion controls are required before any hosted query observation runtime exists?
- What poisoning and spam controls are required before query-derived demand can influence candidate work?
- What operator evidence is required before hosted query-intelligence collection can be enabled?

## P65 Open Questions

P65 adds Candidate Promotion Policy v0 as contract-only governance. Candidate promotion policy is not promotion runtime; candidate confidence is not truth; automatic promotion is forbidden; destructive merge is forbidden; future promotion assessment requires evidence, provenance, source policy, privacy, rights, risk, conflict, human, policy, and operator gates. No candidate, source, evidence, public index, local index, or master-index state is mutated.

## P66 Known Absence Page v0

Known Absence Page v0 is contract-only. It defines scoped absence, not global absence, for future no-result explanations with checked/not-checked scope, near misses, weak hits, gap explanations, safe next actions, privacy redaction, and no download/install/upload/live fetch. Known absence page is not a runtime page yet, not evidence acceptance, not candidate promotion, not master-index mutation, and not telemetry.

<!-- P67-QUERY-PRIVACY-AND-POISONING-GUARD-START -->
## P67 Query Privacy and Poisoning Guard

Open after P67: how future demand dashboard aggregation will prove privacy-filtered, poisoning-checked inputs without telemetry or fake counts; how deletion/export works if a runtime guard store is ever approved; and how operator rate limits remain separate from this contract.
<!-- P67-QUERY-PRIVACY-AND-POISONING-GUARD-END -->

## P68 Open Questions

Future work must define retention/deletion, operator review, source sync, source cache/evidence ledger, connector approvals, and production abuse controls before a real demand dashboard can exist.

## P69 Open Questions

Future work must define source cache/evidence ledger schemas, source-specific User-Agent/contact policy, connector approval packs, credential handling, retention, and worker operations before live sync can run.
