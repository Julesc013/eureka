# H3_OS_PACKAGE_WAVE_QUALITY_DELTA

H3 quality delta is operational evidence only. Use it to compare fixture/live/blocked outputs and gaps. Do not use it for production quality, exhaustive archive coverage, compatibility correctness, installability, rights, or safety claims.

Validation commands: `python scripts/validate_h3_os_package_review_quality_audit.py`, `python scripts/audit_h3_os_package_archive_wave.py --check`, and focused H3 review unit tests.
