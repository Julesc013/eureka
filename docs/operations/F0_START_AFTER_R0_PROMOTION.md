# F0 Start After R0 Promotion

F0 should start from `main` after the R0 dev-to-main promotion succeeds and both remotes are equal.

F0 must use the recovered R0 runtime seams:

- source observation
- source cache
- evidence ledger
- review queue
- reviewed public index
- one-source PyPI metadata live pipeline evidence

F0 must not reintroduce scaffold-only completion.

F0 remains non-deploy unless a future reviewed task explicitly changes that boundary. Promotion to `main` is a canonical repo baseline update only; it is not public launch readiness or production readiness.
