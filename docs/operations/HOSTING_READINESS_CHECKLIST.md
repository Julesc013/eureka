# Hosting Readiness Checklist

Before any future public launch claim, operators need non-claims, safe config defaults, secrets handling, rate limits, observability privacy, incident response, rollback, takedown, kill switches, and launch evidence.

Validation: `python scripts/validate_hosting_readiness.py`.
