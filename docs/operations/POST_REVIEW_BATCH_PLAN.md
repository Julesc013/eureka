# Post Review Batch Plan

Next task: `SEED-BATCH-FRONTIER-MEDIA-00`.

The review-batch runtime makes candidate clusters processable at operator scale. Seed batches can now create candidate groups, use SCOUT trails, and hand selected clusters to batch review before any later local apply or snapshot refresh gate.

Deferred work remains separate: batch UI controls, real operator token handling, local apply execution, snapshot refresh execution, staging, and public launch.
