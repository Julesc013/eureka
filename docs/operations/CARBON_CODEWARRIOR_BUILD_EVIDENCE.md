# Carbon CodeWarrior Build Evidence

Carbon build evidence requires a suitable CodeWarrior Pro 8/9 host.

C-BUNDLE-02 provides source files and `project/Eureka.mcp.README.md` only. It
does not fabricate a binary `.mcp` file.

Future evidence must document manual project creation, source inclusion, build,
launch, fixture display, blocked actions, and diagnostics. Build outputs and
packages remain outside the repository unless a future reviewed task explicitly
allows evidence output.
