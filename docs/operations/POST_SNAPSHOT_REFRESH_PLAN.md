# Post Snapshot Refresh Plan

After `SNAPSHOT-REFRESH-00`, run `PUBLIC-ALPHA-REASSESS-00`.

The reassessment should decide whether the refreshed reviewed/candidate/needs
projection makes the public alpha useful enough for private staging or whether
more seed batches are required. It must keep candidate truth, local apply, and
public launch as separate gates.
