# Post Local Apply Live Metadata Plan

After the temp explicit local-apply proof, the next task is:

```text
SNAPSHOT-REFRESH-03 - Refresh snapshots after local apply of live metadata previews
```

That task should package the limited reviewed metadata/source-lead records in
the snapshot layer without upgrading them into verified artifacts or public
launch material.

After snapshot refresh, a later reassessment should decide whether the public
alpha is still deferred. Reviewed record count may improve, but public launch
still requires separate thresholds and explicit approval.
