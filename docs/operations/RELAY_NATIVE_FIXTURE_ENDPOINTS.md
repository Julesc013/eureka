# Relay Native Fixture Endpoints

Native fixture endpoints are stable read-only JSON projections intended for
future native-client experiments.

Current endpoint kinds cover status, search, object, source, action manifest,
and snapshot manifest. They do not expose admin, write, upload, download,
install, execute, or telemetry routes.

