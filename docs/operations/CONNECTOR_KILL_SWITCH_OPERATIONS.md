# Connector Kill Switch Operations

Future hosted connectors require global and per-connector kill switches before any live behavior. E-BUNDLE-01 defines the requirement only.

Validation: `python scripts/validate_hosting_readiness.py`.
