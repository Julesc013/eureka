# Relay Old Browser Compatibility

Old-browser relay pages use a small static HTML subset. They require no
JavaScript, no fetch/XHR, no login, no cookies, no modern CSS, and no external
images.

The pages are projections of fixture snapshots. They are not hosted public
search pages and do not change public search behavior.

