# Post Live Metadata Pilot Plan

If approval is missing, keep `LIVE-METADATA-PILOT-BATCH-00` as the next task
and provide the approval file.

If the pilot runs successfully, the next task is:

```text
SNAPSHOT-REFRESH-01 - Refresh snapshots after live metadata pilot
```

The following reassessment should remain separate:

```text
PUBLIC-ALPHA-REASSESS-01
```

Live metadata results remain candidates until review, local apply, and snapshot
refresh gates run.
