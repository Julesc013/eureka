# MVP Alpha Readiness Audit

Run the audit with `python scripts/validate_mvp_alpha_audit.py` and `python scripts/audit_mvp_alpha_readiness.py --check`. The audit reads repo-local contracts, examples, and audit packs. It performs no network calls, provider calls, deployments, DNS changes, public binds, or site/dist mutation.
