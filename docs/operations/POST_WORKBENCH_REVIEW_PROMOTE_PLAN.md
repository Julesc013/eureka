# Post Workbench Review Promote Plan

Recommended next task:

`LOCAL-APPLY-GATE-01 — Explicit operator-instance apply, backup, audit, and rollback gate`

That task should add:

- explicit operator instance selection
- dry-run apply preview
- backup and rollback records
- audit logging
- token-gated apply command
- no default mutation path

After the local apply gate, source expansion can proceed with a safer path from live/candidate material to reviewed local projection.
