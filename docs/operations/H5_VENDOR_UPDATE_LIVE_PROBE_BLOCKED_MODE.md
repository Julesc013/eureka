# H5 Vendor/Update Live Probe Blocked Mode

Blocked mode is the default. The CLI emits a blocked result with zero requests and `network_used: false` when approval is missing, a request key is not allowed, a kill switch blocks, an endpoint class is not allowlisted, or a forbidden operation is requested.
