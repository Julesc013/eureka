# Open Questions

- Which existing resolver runtime path should own the next fallback
  implementation?
- Should `INDEXLESS-LIVE-SEARCH-FALLBACK-00` be renamed or kept as the queue ID
  with a preflight suffix for gap audit?
- Does the current public status vocabulary need a live contract migration, or
  only documentation alignment?
- What reviewed artifact record threshold is acceptable after the next
  usefulness evaluation?
- Is Internet Archive metadata the only allowed initial fallback source family,
  or may other already-governed metadata families participate after policy
  gates?
- What is the exact manual launch approval artifact after deferral is lifted?
- Should `.aide/context/latest-task-packet.md` refreshed by `pack` be committed
  with this package or regenerated by the next operator session?

