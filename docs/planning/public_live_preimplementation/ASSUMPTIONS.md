# Assumptions

- The current branch is `dev`.
- `.aide/queue/index.yaml` is the visible queue authority; no
  `.aide/queue/current.toml` was present during inspection.
- `docs/planning/` did not exist before this package, so the prompt's preferred
  output path was created.
- `docs/canon/constitution_v1.md`, `docs/canon/glossary_v1.md`,
  `docs/planning/AUTHORITY_ORDER.md`, and
  `docs/planning/SNAPSHOT_INTAKE_PROTOCOL.md` were absent during inspection.
- `docs/vision/**` is tracked but advisory unless current repo authority
  accepts a claim.
- Proposed JSON in this package is non-live planning material.
- Existing runtime and contract paths are preferred over new roots.

