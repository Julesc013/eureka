# Disable Switches Plan

Required switches:

- fallback disable switch
- source-family disable switch
- review freeze switch
- renderer/profile disable switch
- public safe mode
- Workbench private access off switch

Switch states must be visible to operators and testable in focused validation.

