# Query Privacy Plan

Default posture:

- minimize query retention
- redact sensitive strings where practical
- avoid publishing raw queries unless reviewed/public-safe
- keep operator diagnostics private
- document if public search logs are retained

Fallback runs should record replayable semantic evidence without exposing
private user data publicly.

