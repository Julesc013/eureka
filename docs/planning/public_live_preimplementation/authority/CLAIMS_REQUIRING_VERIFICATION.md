# Claims Requiring Verification

The following claims must be rechecked before implementation or launch:

- Which resolver runtime path should own fallback implementation.
- Whether current semantic statuses exactly match the public status vocabulary
  in this package.
- Whether existing Workbench views expose all fallback run evidence needed by
  operators.
- Whether existing public route models can represent fallback-derived
  candidates and needs without new live contracts.
- Whether current reviewed-record counts and artifact-record quality satisfy a
  renewed launch threshold.
- Whether current external full discovery evidence applies to the latest branch
  after new fallback changes.
- Whether any `docs/vision/**` roadmap claim conflicts with current deferral
  and queue evidence.

