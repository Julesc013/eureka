# Next Task

After this repair is committed, hand off external full discovery rerun 09:

```text
EXTERNAL-FULL-DISCOVERY-RERUN-09
```

After rerun 09 returns compact artifacts, resume with:

```text
SOURCE-SNAPSHOT-FULL-DISCOVERY-INGEST-09
```

Do not run full unittest discovery inside the AI session.

