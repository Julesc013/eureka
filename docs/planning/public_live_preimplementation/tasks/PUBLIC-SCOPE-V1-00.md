# PUBLIC-SCOPE-V1-00

Goal: accept the smallest safe public v1 scope.

Inputs to read first: `public_scope/*.md`, root `README.md`, public alpha
operations docs, launch deferral evidence.

Allowed paths: `docs/planning/**`, `docs/operations/**`, `control/audits/**`.

Protected paths: live contracts and runtime unless a later implementation task
authorizes them.

Deliverables: accepted scope, non-goals, status vocabulary, public action
policy, public page set, visibility model.

Non-goals: public launch, route implementation, source calls, account system.

Validation: docs review plus public non-claim checks if touched.

Exit criteria: public users can see status/evidence/uncertainty and cannot
perform truth-mutating or unsafe actions.

Impact statement: docs/planning impact only unless promoted.

