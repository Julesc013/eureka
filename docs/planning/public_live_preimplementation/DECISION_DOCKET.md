# Decision Docket

## Decisions Captured

1. Keep public v1 read-only and evidence-first.
2. Treat `docs/vision/**` as advisory unless promoted by current repo
   authority.
3. Treat TSIS-00 as completed foundation, not work to duplicate.
4. Implement indexless fallback only as a `ResolutionRunKernel` run mode.
5. Preserve public alpha launch deferral until usefulness and reviewed artifact
   gates pass.
6. Keep proposed contracts under this planning tree until a live contract task
   promotes them.

## Decisions Needed Later

1. Which existing resolution-run runtime path is authoritative for fallback:
   `runtime/resolution_run/**` or `runtime/engine/resolution_runs/**`.
2. Whether current semantic status IDs exactly match the proposed public status
   vocabulary.
3. Final reviewed seed corpus threshold for launch approval.
4. Whether public issue reporting is external-only or a future first-party
   non-mutating channel.
5. Which representation profiles are in alpha scope beyond existing public and
   snapshot routes.

