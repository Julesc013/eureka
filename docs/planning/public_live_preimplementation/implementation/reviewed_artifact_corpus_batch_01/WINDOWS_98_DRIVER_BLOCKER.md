# Windows 98 Driver Blocker

The Windows 98 driver query remains blocked.

Required before any driver artifact recommendation:

```text
hardware vendor
hardware model
device ID or chipset
bus/interface
exact Windows version
architecture
machine/board model
known source/media context
```

Recommended blocker task:

```text
USER-HARDWARE-DETAILS-00
```

No random driver artifact should be recommended without those details.

