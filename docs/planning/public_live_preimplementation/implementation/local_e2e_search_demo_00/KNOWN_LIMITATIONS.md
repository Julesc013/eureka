# Known Limitations

```text
local proof only
fixture-backed, not evidence-backed
no live provider wiring
no arbitrary web search
no downloads
no reviewed/public/master index mutation
no public alpha readiness claim
no verified artifact claim
```

The demo shows honest current behavior over local hard-query fixtures. It does
not grow the reviewed corpus and does not replace external artifact evidence.

