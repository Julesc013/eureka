# Renderer Coverage

Covered profiles:

```text
json_v0
text_v0
html_basic_v0
snapshot_v0
```

Coverage claims:

```text
status visible: yes
evidence/gap posture visible: yes
artifact gate visible: yes
operator-only actions stripped from public output: yes
HTML escapes unsafe fixture text: yes
snapshot output deterministic: yes
unknown status degrades safely: yes
```

The demo uses the current baseline renderers. It does not add renderer behavior.

