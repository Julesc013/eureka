# REVIEWED-CORPUS-SEED-BATCH-01

This task consolidates `MANUAL-OBSERVATION-BATCH-00` and `HUMAN-REVIEW-BATCH-00` into a reviewed seed-corpus batch.

Primary output:

```text
evals/hard_queries/reviewed_seed_corpus/batch_01/
```

Status: `PASS_WITH_WARNINGS`.

The warning is intentional: the corpus now has two review-event-backed seed records, but the public alpha corpus gate remains `FAIL_INSUFFICIENT_REVIEWED_CORPUS`.
