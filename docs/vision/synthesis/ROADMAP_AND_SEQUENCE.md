# Roadmap and Sequence

## Layer 0 — Archive synthesis

1. Finish `PROJECT-VISION-CORPUS-01`.
2. Review `ULTIMATE_PROJECT_VISION_DRAFT.md`, `CURRENT_PROJECT_REALITY.md`, `DECISION_DOCKET.md`, `CONTRADICTIONS_AND_DRIFT.md`, and `ROADMAP_AND_SEQUENCE.md`.
3. Run `PROJECT-VISION-BOOK-01` only after the synthesis layer is reviewed.

## Layer 1 — Current-state verification

1. Inspect current repo authority files and queue state.
2. Verify branch status and promoted task outputs.
3. Resolve stale/generated-state conflicts.
4. Run source/snapshot baseline closeout if it is still the current queue gate.
5. Keep full unittest discovery outside AI; AI should read compact summaries.

## Layer 2 — Semantic and representation foundation

1. Add TSIS doctrine documents.
2. Add semantic contracts: entity, status, action/affordance, badge, navigation, relationship.
3. Add view-model contracts: search page, result card, object, need, candidate, source, evidence, status, review, compare, account.
4. Add representation contracts: profile, renderer contract, skin contract, compatibility budget, fallback rule, cache key.
5. Add compatibility/golden cross-render tests.

## Layer 3 — Resolution and Workbench proof

1. Establish or verify ResolutionRunKernel.
2. Define Search Interaction Contract.
3. Build Workbench foundation over the same run/view packets.
4. Add result lanes, events, controls, coverage, absence, and blocked-action posture.
5. Add browser-visible IA metadata lane with explicit budgets and no downloads.
6. Add review/promote UI, local apply gate, index rebuild, and rerun proof.

## Layer 4 — Evaluation and domain generalization

1. Run SYN against visible Workbench behavior, not only backend fixtures.
2. Add DOMAIN pack architecture.
3. Add SCOUT relation-guided discovery.
4. Resume F extraction as hidden-member discovery only after policy/sandbox limits are ready.

## Layer 5 — Distribution and product surfaces

1. Build snapshots and relay endpoints as stable consumption boundaries.
2. Add public web as a restricted projection of reviewed data.
3. Add CLI/TUI/API surfaces over the same packets.
4. Add native/mobile clients only as snapshot/relay/API consumers.
5. Reconsider public hosting and marketplace behavior only after operations, rights, malware/risk, abuse, privacy, takedown, monitoring, and rollback gates exist.
