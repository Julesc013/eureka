# Snapshot Reviewed Source Lead Section

Schema: `contracts/snapshot/snapshot_reviewed_source_lead_section.v0.json`

Required posture:

- `reviewed_source_lead_count` records are projected from local apply
- `limited_claim_scope` is source-lead-only
- `artifact_verified` is `false`
- `verified_download_claim` is `false`
- `malware_clean_claim` is `false`
- `rights_clearance_claim` is `false`

Source leads support follow-up review. They are not verified software or launch claims.
