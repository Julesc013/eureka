# Snapshot Reviewed Metadata Preview Section

The reviewed metadata preview section contains preview records that may be eligible for local apply.

The section must carry `accepted_truth: false` and `local_apply_required: true`. It does not mutate reviewed, master, or public indexes.
