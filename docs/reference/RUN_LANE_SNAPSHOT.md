# Run Lane Snapshot

`run_lane_snapshot.v0` wraps an existing Workbench result-lane page for a
specific run and projection profile.

The snapshot is projection output only. It does not create truth, execute
WorkUnits, or mutate any index.
