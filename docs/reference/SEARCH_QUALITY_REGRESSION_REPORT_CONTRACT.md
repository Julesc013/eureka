# Search Quality Regression Report Contract

`search_quality_regression_report.v0` compares shadow ranking outputs against fixture query sets with scoped metrics.

Reports must not claim production search quality, external superiority, exhaustive coverage, rights clearance, malware safety, or verified installability.

Validation: `python scripts/validate_ranking_shadow_runtime.py`.
