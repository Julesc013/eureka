# Runtime Config Boundary Contract

Defines safe defaults and fail-closed behavior for risky hosting flags such as live probes, downloads, uploads, accounts, telemetry, public index writes, master index writes, and public relay bind.

Validation: `python scripts/validate_hosting_readiness.py`.
