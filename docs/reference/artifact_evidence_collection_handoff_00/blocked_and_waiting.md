# Blocked And Waiting

Current waiting status:

```text
WAITING_FOR_EXTERNAL_ARTIFACT_EVIDENCE
```

Secondary waiting status:

```text
WAITING_FOR_USER_HARDWARE_DETAILS
```

Still blocked:

- public alpha
- `dev -> main` promotion
- verified artifact claims
- Windows 98 driver recommendation

Reason:

```text
No-call review over carried references cannot grow the reviewed artifact count. New compact evidence must be collected externally or manually before the next observation/review batch can be meaningful.
```
