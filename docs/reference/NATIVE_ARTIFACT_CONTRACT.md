# Native Artifact Contract

`contracts/native/native_artifact.v0.json` describes planned native artifact names and build host requirements.

For C-BUNDLE-01, artifact entries are plans or future naming patterns. `produced_current` and `production_release_current` remain false. No binary, installer, release package, or signed release is created by this bundle.

Artifact records are build evidence metadata, not product distribution claims.
