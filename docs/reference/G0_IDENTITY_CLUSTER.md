# G0 Identity Cluster

G0 identity cluster candidates group fixture records for review. Candidate group
types include same object, same version, same release, same representation,
mirror, duplicate, near miss, wrong platform, wrong version, parent bundle,
member, and related but distinct.

Clusters remain provisional. They preserve member refs and source-specific
locators. Accepted identity merges are disabled.
