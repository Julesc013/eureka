# H6 Public Document Trace Policy

Public document traces are manifest-only by default for restricted or sensitive sources. Legal, privacy, safety, and rights review is required before any connector work.

Validation: `python scripts/validate_h6_web_archive_news_event_policy_packs.py`.
