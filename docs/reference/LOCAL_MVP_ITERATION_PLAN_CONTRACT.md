# Local MVP Iteration Plan Contract

`local_mvp_iteration_plan.v0` records the local non-deploy next-wave decision after the MVP alpha and public-alpha planning gates. It is a routing contract, not an implementation or launch claim.

Required fields include the current MVP status, evaluated options, recommended next task, deployment deferral, blockers, warnings, truth boundary, product boundary, no-goals, and notes.
