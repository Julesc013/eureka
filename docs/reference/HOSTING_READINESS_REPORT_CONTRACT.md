# Hosting Readiness Report Contract

Aggregates non-claims, runtime config, secrets, rate limits, observability, incident, rollback, takedown, kill switch, and launch evidence status.

Validation: `python scripts/validate_hosting_readiness.py`.
