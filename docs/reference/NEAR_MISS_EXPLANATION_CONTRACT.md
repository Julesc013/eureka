# Near Miss Explanation Contract

`near_miss_explanation.v0` explains why a fixture result is useful but not exact, such as wrong version, wrong platform, weak evidence, or extraction-needed cases. Near misses do not reject, merge, accept, or rank results automatically.

Validation: `python scripts/validate_search_explanation_runtime.py`.
