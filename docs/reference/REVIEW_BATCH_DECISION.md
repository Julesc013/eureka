# Review Batch Decision

`review_batch_decision.v0` records a validated batch decision preview.

Required safety fields include `operator_context_required`, `allowed`, `blocked_reasons`, `creates_accepted_truth: false`, `review_required: true`, and `accepted_truth: false`.
