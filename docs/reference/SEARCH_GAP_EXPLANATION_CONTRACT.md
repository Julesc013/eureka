# Search Gap Explanation Contract

`search_gap_explanation.v0` turns extraction or search-gap previews into reviewable explanations of why a result may be missing or incomplete. Suggested next actions and WorkUnit seeds are future-only and review-gated.

Validation: `python scripts/validate_search_explanation_runtime.py`.
