# Snapshot Reviewed Metadata Record Section

Schema: `contracts/snapshot/snapshot_reviewed_metadata_record_section.v0.json`

Required posture:

- `reviewed_metadata_record_count` records are projected from local apply
- `limited_claim_scope` is metadata-only
- `artifact_verified` is `false`
- `verified_download_claim` is `false`
- `malware_clean_claim` is `false`
- `rights_clearance_claim` is `false`

This section is read-only snapshot material and not public/master index mutation.
