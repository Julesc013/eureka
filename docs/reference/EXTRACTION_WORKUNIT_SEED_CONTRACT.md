# Extraction WorkUnit Seed Contract

`extraction_workunit_seed.v0` proposes future reviewed WorkUnits such as `deepen_container_future`, `verify_manifest_candidate_future`, or `check_member_relevance_future`. Current seeds do not execute WorkUnits, fetch files, inspect private paths, or mutate runtime state.
