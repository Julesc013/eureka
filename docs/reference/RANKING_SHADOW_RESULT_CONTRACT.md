# Ranking Shadow Result Contract

`ranking_shadow_result.v0` records fixture-only proposed ordering. It does not publish order, mutate ranking, accept evidence, accept candidates, or mutate public/master indexes.

Current outputs are local shadow artifacts for review and regression only.

Validation: `python scripts/validate_ranking_shadow_runtime.py`.
