# Ranking Factor Contract

`ranking_factor.v0` defines deterministic factor families such as identifier, version, platform, source lane, evidence support, extraction match, near-miss penalty, and policy block penalty.

Factors are heuristics. They are not truth and require review before any public use.

Validation: `python scripts/validate_ranking_shadow_runtime.py`.
