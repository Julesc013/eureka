# Current External Discovery State

External artifact root:

```text
../eureka-test-runs/source_snapshot_full_discovery_rerun_05/
```

Compact artifacts ingested:

| Artifact | Status |
|---|---|
| `status.json` | present |
| `full_unittest_summary.json` | present |
| `failure_families.json` | present |
| `failed_tests.txt` | present |
| `environment.json` | present |

Raw stdout and stderr were not copied into the repo.

The run is terminal:

```text
status: fail
exit_code: 1
pid: null
```

The failed-test inventory contains 39 failed tests across 34 failed modules.

