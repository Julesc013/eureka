# Terminal Profile Contract

`contracts/relay/terminal_profile.v0.json` describes line-mode text output.

The terminal profile uses numbered menus, no mouse requirement, no color
requirement, and no terminal escape requirement by default. It preserves the
same identity, posture, action, and limitations fields as the other renderers.

