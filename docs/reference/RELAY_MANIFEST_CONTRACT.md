# Relay Manifest Contract

`contracts/relay/relay_manifest.v0.json` lists available fixture routes,
profiles, snapshot references, blocked actions, no-goals, and limitations.

The manifest is read-only. It does not activate public routes, host traffic,
download files, execute actions, or mutate public/master indexes.

