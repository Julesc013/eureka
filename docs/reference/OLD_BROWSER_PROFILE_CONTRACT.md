# Old Browser Profile Contract

`contracts/relay/old_browser_profile.v0.json` describes the legacy HTML target.

D-BUNDLE-02 uses no required JavaScript, no required modern CSS, no image
dependency, UTF-8 text, and semantic fields that match text/terminal renderers.
GET forms may be described for navigation, but no write methods are enabled.

