# H8 Manuals Docs Fixture Runtime

H8 fixture runtime converts committed synthetic manuals/docs/standards metadata fixtures into candidate-only normalized records and replay reports. It does not enable live access, catalog/API queries, downloads, OCR/full-text extraction, scraping, crawling, restricted-source access, action execution, or truth acceptance.

This prepares H8-BUNDLE-03 by proving parser and boundary behavior with fixture-equivalent outputs only. Validation uses the H8 fixture validator, normalizer check, replay check, summary check, targeted tests, full unittest discovery, and architecture-boundary checks.
