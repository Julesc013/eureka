# Review Batch Packet

`review_batch_packet.v0` contains candidate refs, cluster refs, cluster summaries, allowed decision names, and empty decision/handoff slots.

It is review-required and keeps `accepted_truth` false. Public views may read summaries but cannot make decisions.
