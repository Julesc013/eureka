# H6 Web Capture Identity Policy

Web capture identity candidates include original URL, capture URL, timestamp, digest, status, MIME type, CDX key, WARC reference, collection ID, redirects, and dead-link candidates. Capture presence is not completeness or authenticity proof.

Validation: `python scripts/validate_h6_web_archive_news_event_policy_packs.py`.
