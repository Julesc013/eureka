# Extraction Review Seed Contract

`extraction_review_seed.v0` turns a member, manifest, source-locator, or blocked extraction finding into a local review seed preview. A seed is not a review decision and does not write to the review queue.
