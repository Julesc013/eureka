# Known Absence Record Contract

`known_absence_record.v0` describes scoped local absence. It must list sources checked, sources not checked, remaining work, and limitations. It must not claim global absence or exhaustive web search.

Validation: `python scripts/validate_search_explanation_runtime.py`.
