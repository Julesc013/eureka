# Local MVP Expansion Gate Contract

`local_mvp_expansion_gate.v0` records the gate posture for source expansion, risky actions, semantic/AI assistance, wider clients, deployment, and remediation. Remediation overrides expansion when blockers exist.

H2 source expansion is allowed only as policy-pack work that follows H0/H1 patterns and keeps live calls, source sync, downloads, and truth acceptance disabled.
