# Batch Promotion Preview

`batch_promotion_preview.v0` describes a possible local-reviewed record preview for operator review.

It is not promotion. It requires separate local apply and snapshot refresh gates before any reviewed or public projection can change.
