# Discovery Trail

`discovery_trail.v0` records the path of SCOUT relation steps from a seed
candidate. It can be projected publicly as read-only explanation, but it cannot
accept candidates, promote records, or mutate indexes.
