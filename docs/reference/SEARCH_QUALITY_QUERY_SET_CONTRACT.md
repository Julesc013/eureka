# Search Quality Query Set Contract

`search_quality_query_set.v0` groups fixture query cases, expected local results, expected near misses, expected absence records, scope, and no-claims boundaries.

Query sets are local regression fixtures, not production quality proof.

Validation: `python scripts/validate_ranking_shadow_runtime.py`.
