# Hosted Wrapper Rehearsal Model

Hosted wrapper rehearsal is a local compatibility rehearsal over fixtures. It never deploys, binds public interfaces, calls providers, mutates site/dist, or changes public search behavior.

Validation commands are listed in the E-BUNDLE-02 audit pack. All examples preserve no deployment, no provider call, no DNS change, no site/dist mutation, no live fanout, and no production claim.
