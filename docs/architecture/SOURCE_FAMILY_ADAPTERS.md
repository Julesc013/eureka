# SOURCE FAMILY ADAPTERS

Each source family exposes a manifest, fixture/mock transport, normalizer, mapping-plan support, and boundary report through the generic SourceActionKernel adapter interface.

This document describes metadata fixture/mock behavior only. It does not authorize live source calls, scraping, downloads, extraction, public source fanout, or production/public launch readiness.
