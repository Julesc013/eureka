# NATIVE FIRST WAVE INTEGRATION MODEL.md

The first-wave integration model covers WinForms, Win32, AppKit, and Carbon as read-only consumers of snapshot, relay, action, and blocked-action contracts. Track C exits when these lanes are represented and risky behavior remains disabled.

The model keeps native clients as consumers, not resolvers.
