# Source Action Policy Gate

Every source action passes through policy before transport. Fixture and mock transports are deterministic and non-mutating. Operator-approved live transport is disabled by default and remains future work requiring redaction policy, operator approval, rate limits, and boundary reporting.
