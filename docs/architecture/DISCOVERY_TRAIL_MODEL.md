# Discovery Trail Model

A discovery trail is an ordered explanation of candidate relations from one seed
candidate to related candidates or follow-up paths.

Each step references a relation id, relation type, source candidate ref, target
candidate ref, and explanation. Trail confidence is only a review hint. Evidence
refs remain empty until a separate review workflow creates evidence.
