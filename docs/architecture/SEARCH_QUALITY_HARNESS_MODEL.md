# Search Quality Harness Model

The search-quality harness reads fixture query sets and shadow ranking outputs, then emits scoped regression reports.

The harness measures local fixture behavior only. It does not call networks, models, providers, browsers, live sources, or public search runtimes.
