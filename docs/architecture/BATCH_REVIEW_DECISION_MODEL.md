# Batch Decision Model

Batch decisions require operator context or explicit dry-run mode.

Supported decisions include useful-lead marking, duplicate marking, rejection classes, more-evidence requests, blocking, deferral, and `accept_local_reviewed_preview`.

Decision validation returns a preview record. It does not update candidate storage, reviewed indexes, master indexes, public indexes, or operator instances.
