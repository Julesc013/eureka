# NATIVE PACKAGING MODEL.md

The native packaging model separates source skeletons, manual build evidence, artifact manifests, and release-candidate previews. C-BUNDLE-03 only creates auditable manifests and examples; it does not package binaries.

The model keeps native clients as consumers, not resolvers.
