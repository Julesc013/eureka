# Source Action Kernel

The Source Action Kernel is the generic policy-gated seam for source interactions. It turns a query context into request plans, fixture or mock transport results, normalized observations, mapping plans, lane projection plans, review handoff plans, boundary reports, and scorecards. Source actions are not truth and do not mutate reviewed, master, public, or operator-instance stores. Live calls are disabled by default.
