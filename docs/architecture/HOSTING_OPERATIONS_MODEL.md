# Hosting Operations Model

Hosting readiness is an operational claim boundary. This model defines what must exist before deployment while leaving deployment, provider calls, public backend enablement, live fanout, and production claims disabled.

Validation: `python scripts/validate_hosting_readiness.py`.
