# H6 Web Archive News Event Model

H6 extends the Source OS into trace/evidence-oriented web archive, news, event, newspaper, broadcast, dead-link, and public document metadata. The current status is policy-pack-only.

Validation: `python scripts/validate_h6_web_archive_news_event_policy_packs.py`.
