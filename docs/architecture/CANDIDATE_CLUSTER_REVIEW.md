# Candidate Cluster Review

Candidate clusters are built from local CandidateRecord data and SCOUT relations.

Cluster kinds cover duplicates, near misses, shared source family, shared collection, shared domain, shared platform, shared format family, provenance chains, and review-priority groups.

Clusters are operator navigation aids. They are not truth claims and do not imply promotion readiness.
