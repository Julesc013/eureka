# Eval Tests

`tests/evals/` verifies repo-level evaluation assets that are shared across
future subsystems.

Bootstrap keeps these checks intentionally light. They validate that governed
eval corpora stay machine-readable and structurally useful before later ranking,
investigation, or AI work depends on them.
