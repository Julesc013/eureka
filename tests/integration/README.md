# Integration Tests

This directory is reserved for cross-component integration coverage.

Bootstrap keeps it as scaffolding only. Real suites should begin once gateway, engine, and surface behavior become concrete enough to exercise together.

