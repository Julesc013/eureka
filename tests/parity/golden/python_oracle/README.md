# Python Oracle Golden Outputs

Python remains Eureka's executable oracle. This directory contains committed
golden-output packs generated from Python behavior for future Rust parity work.

Current pack:

- `v0/`: first Rust Parity Fixture Pack v0
