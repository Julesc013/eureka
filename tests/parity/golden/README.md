# Golden Parity Fixtures

`tests/parity/golden/` stores committed Python-oracle outputs for future Rust
candidate comparisons.

These fixtures are not Rust behavior and are not a complete parity runner. They
are stable reference outputs generated from the current Python lane.
