# Manual Observation Review Backlog Batch 00

Task ID: `MANUAL-OBSERVATION-BATCH-00`

This directory mirrors the review handoff from
`evals/hard_queries/manual_observations/batch_00/` in a queue-oriented shape.

JSON is used instead of YAML because the current hard-query eval lane is
stdlib-only.

No review ledger decisions, review events, reviewed records, or index mutations
are created here.
