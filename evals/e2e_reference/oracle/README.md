# E2E Reference Oracle

This directory contains deterministic, inspectable evaluation cases for the
Eureka local reference system. The oracle composes existing local fixtures and
product seams; it is not a model judge, runtime engine, public service, or full
unittest discovery replacement.

Generated runs are written under `.eureka/e2e-reference/eval/<execution-id>/`.
