# .NET Library Lane

Lane id: `lib.dotnet`.

This is a placeholder for future shared .NET helpers over governed snapshot,
relay, and action contracts. C-BUNDLE-01 only adds the WinForms proof project.
