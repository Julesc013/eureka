# Win32 Build Notes

This lane targets Visual C++ 6.0 style hosts. C-BUNDLE-02 adds source and
project skeleton files only.

Manual build evidence may be collected later under explicit audit output. Build
outputs, intermediates, binaries, installers, and packages are not committed.
