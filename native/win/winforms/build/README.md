# WinForms Build Notes

This directory is reserved for future build evidence notes. Build outputs,
intermediate files, binaries, package restore artifacts, and generated logs are
not committed in C-BUNDLE-01.
