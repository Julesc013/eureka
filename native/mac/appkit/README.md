# AppKit Lane

Lane id: `mac.appkit`.

This lane is a skeleton for future AppKit clients. It consumes snapshot, relay,
and action-manifest contracts only. No Xcode project files are added in
C-BUNDLE-01.
