# AppKit Build Notes

This lane targets a future Xcode 9.x manual build. C-BUNDLE-02 adds Objective-C
source and a project placeholder only.

Build outputs, derived data, archives, signed bundles, packages, and logs are
not committed.
