# Minimal GitHub Release Metadata Summary

Synthetic P98 source-cache dry-run candidate for GitHub Releases-like metadata.
It uses a synthetic repository label and performs no API call, clone, release
asset fetch, token use, connector runtime, cache write, or index mutation.
