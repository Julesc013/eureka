# Minimal Package Metadata Observation

Synthetic P99 evidence-ledger dry-run candidate for package metadata. It does
not call a package registry, install anything, inspect package payloads, accept
claims as truth, or write evidence state.
