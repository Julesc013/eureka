# PyPI Metadata Connector Approval Example v0

This directory contains a synthetic P74 approval record and manifest for a future PyPI metadata connector. It is not live source data and was not fetched from PyPI.

The example states that connector runtime implementation, approval now, live source calls, external calls, PyPI API calls, package metadata fetches, release fetches, file metadata fetches, wheel downloads, sdist downloads, package file downloads, package installs, dependency resolution, package archive inspection, public-query fanout, source cache mutation, evidence ledger mutation, candidate/public/local/master index mutation, credentials, PyPI tokens, telemetry, downloads, mirroring, installs, and execution are all false.

The package name is a synthetic placeholder. User-Agent/contact, source policy, package identity, dependency metadata caution, token policy, rate-limit, timeout, retry, circuit breaker, approval checklist, and operator checklist remain pending.
