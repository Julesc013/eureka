# Minimal Manual Observation Probe Queue Item v0

This example is a synthetic Probe Queue v0 item for a future human-operated
manual observation. It is not an executed probe, not a runtime queue item, not
source evidence, and not master-index truth.

The item keeps live probing disabled, stores no raw query, writes no runtime
state, and only references synthetic public-safe query-intelligence records.
