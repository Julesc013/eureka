# Minimal Deep Extraction Probe Queue Item v0

This example is a synthetic future offline extraction planning item for a
committed fixture/source-cache style object. It is not broad local ingestion,
does not execute a tool, and does not write evidence, candidates, source cache,
or indexes.

The example requires future approval, bounded tooling, validation, and review
before any extracted evidence could be reused.
