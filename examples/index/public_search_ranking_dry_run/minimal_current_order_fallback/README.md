# Minimal Current Order Fallback

Synthetic public-safe result set that intentionally requires current-order
fallback because factor data is missing. Strict mode treats this as a failure;
non-strict mode reports the fallback without mutation.

