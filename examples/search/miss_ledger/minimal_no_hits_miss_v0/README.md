# Minimal No-Hits Miss v0

This synthetic P61 example records a scoped no-hit miss for
`no-such-local-index-hit`.

It retains no raw query, stores no user identifier, makes no global absence
claim, creates no search need, enqueues no probe, mutates no cache or index, and
performs no external call.
