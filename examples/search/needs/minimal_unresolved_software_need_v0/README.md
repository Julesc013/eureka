# Minimal Unresolved Software Need v0

Synthetic P62 search need example for a Windows 7 compatible application need.
It is public-safe example data only: no raw query, user identifier, private
path, sensitive token, runtime need store, probe job, candidate record, or master-index
mutation is present.
