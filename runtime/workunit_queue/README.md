# runtime.workunit_queue Compatibility Shim

Canonical package: `runtime.worker.workunit_queue`.

This directory is retained only so historical imports such as `runtime.workunit_queue` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
