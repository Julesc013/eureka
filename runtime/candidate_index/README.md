# runtime.candidate_index Compatibility Shim

Canonical package: `runtime.index.candidate`.

This directory is retained only so historical imports such as `runtime.candidate_index` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
