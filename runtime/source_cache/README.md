# runtime.source_cache Compatibility Shim

Canonical package: `runtime.source.cache`.

This directory is retained only so historical imports such as `runtime.source_cache` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
