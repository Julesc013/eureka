# runtime.review_queue Compatibility Shim

Canonical package: `runtime.review.queue`.

This directory is retained only so historical imports such as `runtime.review_queue` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
