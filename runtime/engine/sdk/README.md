# Engine SDK

This directory is reserved for a future narrow engine SDK.

It should only be used if Eureka later adopts an explicit offline or local native mode. Bootstrap keeps it as a boundary marker, not as an implementation commitment.

