# Ingest Interface

`runtime/engine/interfaces/ingest/` is reserved for the narrow ingest-facing engine boundary that connectors may target.

Connectors may adapt into this path, but they must not define canonical object semantics here.

