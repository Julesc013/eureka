# Service Engine Interface

`runtime/engine/interfaces/service/` is reserved for engine service interfaces consumed by runtime gateway components behind the public API boundary.

These interfaces are distinct from the public product contract and remain draft until real runtime behavior exists.

