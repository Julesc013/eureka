# Public Engine Interface

`runtime/engine/interfaces/public/` is reserved for engine-facing interfaces that gateway may expose or translate through the governed public API boundary.

Bootstrap adds the boundary only. It does not define stable service semantics yet.

