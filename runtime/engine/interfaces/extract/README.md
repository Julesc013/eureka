# Extract Interface

`runtime/engine/interfaces/extract/` is reserved for the narrow extract-facing engine boundary that connectors may target.

Bootstrap keeps this path descriptive and empty beyond the boundary marker.

