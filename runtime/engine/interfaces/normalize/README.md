# Normalize Interface

`runtime/engine/interfaces/normalize/` is reserved for the narrow normalize-facing engine boundary that connectors may target.

Trust semantics and canonical object meaning remain governed elsewhere under `contracts/`.
