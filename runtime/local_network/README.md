# runtime.local_network Compatibility Shim

Canonical package: `runtime.local.network`.

This directory is retained only so historical imports such as `runtime.local_network` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
