# runtime.local_review Compatibility Shim

Canonical package: `runtime.local.review`.

This directory is retained only so historical imports such as `runtime.local_review` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
