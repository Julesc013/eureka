# runtime.evidence_ledger Compatibility Shim

Canonical package: `runtime.evidence.ledger`.

This directory is retained only so historical imports such as `runtime.evidence_ledger` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
