# runtime.public_index Compatibility Shim

Canonical package: `runtime.index.public`.

This directory is retained only so historical imports such as `runtime.public_index` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
