# runtime.local_service Compatibility Shim

Canonical package: `runtime.local.service`.

This directory is retained only so historical imports such as `runtime.local_service` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
