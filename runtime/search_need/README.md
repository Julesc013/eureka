# runtime.search_need Compatibility Shim

Canonical package: `runtime.search.need`.

This directory is retained only so historical imports such as `runtime.search_need` continue to resolve. It must contain only `__init__.py` and this README marker, and it must not become an implementation home.
