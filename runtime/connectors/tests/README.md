# Connector Tests

This directory is for component-local connector tests.

Connector coverage here should validate adapter behavior against governed contracts and narrow engine-facing interfaces, not define separate semantics.
