# Instance Config Schema

`config/instance.json` records instance identity, `instance_schema_version: 1`, timestamps, explicit root, local appliance mode, stores, policies, warnings, limitations, and disabled server/LAN/product-claim flags.

Unsupported versions fail closed during validation.
