# Migration State Schema

`config/migration_state.json` records current and target instance schema versions, migration-needed state, apply permission, destructive migration requirement, backup/rollback posture, migration history, blockers, and warnings.

LOCAL-02 keeps migration apply disabled.
