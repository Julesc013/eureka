# Artifact Matrix

Default local root: `.aide.local/test-runs/<run-id>/`.

The harness writes stdout, stderr, exit code, compact summary, failure families,
failed tests, touched paths, and environment metadata. Raw logs are not
committed by default.
