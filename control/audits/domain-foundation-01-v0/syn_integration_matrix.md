# SYN Integration Matrix

Each seed DOMAIN pack maps to deterministic SYN query examples.

The mappings are fixtures for evaluation pressure only. They do not create
runtime query logs, evidence, SearchNeeds, WorkUnits, or reviewed records.
