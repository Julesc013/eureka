# Query Hint Matrix

The DOMAIN query hint matrix records promote terms, suppress terms, and
source-family preferences for each seed domain.

The hints are read-only interpretation data. They do not perform live source
calls, source probes, downloads, extraction, model calls, or evidence creation.
