# Representation And Member Identity Model

Representation/member identity supports same, different, variant, parent-child, member-of-container, unknown, and conflicting relations. Payloads and downloads are disabled.
