# H2 Connector Family Summary

All H2 sources map to the known `package_registry` connector family with `api_json` as an alternate fixture shape. OCI registry metadata is noted as a future specialization candidate, not a current live connector.
