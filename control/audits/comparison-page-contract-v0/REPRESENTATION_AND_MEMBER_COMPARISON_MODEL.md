# Representation And Member Comparison Model

Representations and members are metadata-only in v0. `payload_included` and `downloads_enabled` are false. Member paths must be public-safe and cannot be private filesystem paths.
