# Validation Matrix

The SCOUT runtime validator and focused tests cover relation inference,
discovery trails, related paths, source trust observations, WorkUnit seed
suggestions, candidate integration, public projection boundaries, and CLI smoke.
Full unittest discovery is not run by policy.
