# AppKit Skeleton Summary

C-BUNDLE-02 adds an Objective-C AppKit read-only skeleton under
`native/mac/appkit/`.

Added scope:

- app delegate and main entry
- main window controller
- read-only search view
- snapshot and relay adapters
- documented `Eureka.xcodeproj` placeholder

The Xcode project is not fabricated as a verified build graph. Build evidence is
manual/future.
