# Fixture Normalization Report

Five committed IA-shaped fixtures were added:

- minimal item metadata
- software item metadata
- manual item metadata
- multi-file item metadata
- policy-blocked item metadata

Each fixture is synthetic or public-safe, uses no network/API/model/provider
calls, and includes false truth/product boundary booleans. Expected normalized
records are stored under `examples/connectors/internet_archive/normalized/` and
are generated by `scripts/normalize_ia_metadata_fixture.py`.

The policy-blocked fixture remains blocked in its normalized `policy` section.
