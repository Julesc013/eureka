# Snapshot File Tree Render Report

The file-tree renderer emits README/index-style text that names the manifest and record entries.

The view preserves required semantic posture fields and remains descriptive. It does not create a relay, hosted file tree, deployment, public route, or mirror.
