# Snapshot Consumer Report

The consumer helper loads explicit fixture snapshot artifacts and emits a local/offline consumer report.

The report lists records loaded, manifest verification posture, fixity posture, signature status, available render targets, blocked actions, limitations, and boundaries. It does not activate relay, hosting, public routes, or live access.
