# Snapshot Lite HTML Render Report

The lite HTML renderer emits static escaped HTML fragments for fixture snapshots.

The output is an offline projection only. It does not activate public routes, load external assets, host content, relay requests, mutate public search, or regenerate `site/dist`.
