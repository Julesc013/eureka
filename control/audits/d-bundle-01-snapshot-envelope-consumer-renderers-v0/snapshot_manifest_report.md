# Snapshot Manifest Report

Snapshot manifests are deterministic local projections of explicit fixture records.

The sample manifests cover minimal, search, object, source/need/action, and policy-blocked snapshot inputs. They carry record counts, type counts, source/evidence/action summaries, render targets, fixity entries, signature posture, limitations, and no-claims.

Manifests remain fixture artifacts and do not publish or mutate indexes.
