# Connector Scope

Scope is release metadata-only: package release summaries, tag metadata summaries, wheel/sdist/package file metadata summaries, latest release metadata, release date/version summaries, and prerelease/draft status summaries after approval.
