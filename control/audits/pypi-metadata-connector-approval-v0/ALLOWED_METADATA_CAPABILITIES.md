# Allowed Metadata Capabilities

Allowed capabilities are future-after-approval only and disabled now: package release metadata, release tag metadata, wheel/sdist/package file metadata summary, latest release metadata, release date/version summary, and prerelease/draft status summary.
