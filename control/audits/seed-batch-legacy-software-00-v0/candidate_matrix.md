# Candidate Matrix

The fixture run produces 16 review-only candidate summaries and a local
candidate-index example. Candidate summaries include visible suppression
reasons and blocked distribution actions.
