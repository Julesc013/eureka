# Sample Summary

LOCAL-11 confirms that localhost remains the default, LAN binding requires
`--bind-lan`, and LAN clients can only reach read-only service/workbench routes.

Unsafe LAN route classes are blocked, and actual cross-device LAN smoke is
deferred to LOCAL-12.
