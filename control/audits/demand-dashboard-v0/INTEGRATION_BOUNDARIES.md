# Integration Boundaries

Future dashboard aggregation must consume only privacy-filtered and poisoning-guarded query-intelligence outputs. Public search is not wired to write dashboard snapshots, persist analytics, expose real demand, or alter ranking in P68.
