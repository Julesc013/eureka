# Conflict And Dispute Model

Queue entries preserve conflict status, conflicting references, and dispute
notes. Conflicts block automatic acceptance and should remain visible to future
reviewers instead of being flattened into a single truth claim.

The synthetic example records `none_detected` and states that no dispute is
represented.
