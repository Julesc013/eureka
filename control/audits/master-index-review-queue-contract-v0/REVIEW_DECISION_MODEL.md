# Review Decision Model

Review decisions may accept public, reject, quarantine, request revision,
supersede, or defer. An `accept_public` decision is limited and
provenance-bound; it is not rights clearance, malware safety, or canonical
truth.

The synthetic example uses `defer`, so it publishes no public claims and mutates
no master index.
