# Connector Family Registry Summary

The connector family registry covers API JSON, package registry, OAI-PMH, IIIF,
WARC/CDX, HTML catalog, RSS/Atom/sitemap, directory listing, storefront, local
source, review text, and restricted manifest families.

Every family defaults to no live access and requires fixture replay plus policy
gates before future live use.
