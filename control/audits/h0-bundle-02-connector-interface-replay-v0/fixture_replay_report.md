# Fixture Replay Report

Fixture replay is enabled only for committed offline fixtures. It records
`no_network_used: true` and `no_live_source_used: true`.

Fixture replay output is a connector output candidate. It is not accepted source
truth, accepted evidence, a public record, or an index mutation.
