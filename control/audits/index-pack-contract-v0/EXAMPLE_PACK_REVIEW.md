# Example Pack Review

Example path:

`examples/index_packs/minimal_index_pack_v0/`

The example pack is synthetic and self-authored. It includes:

- one manifest
- one privacy/rights document
- one index summary
- one source coverage summary
- four record summaries
- one field coverage file
- two query examples
- SHA-256 checksums

It contains no raw SQLite database, raw cache, executable, installer, binary
artifact, private local path, raw source payload, live URL, or long copyrighted
text.
