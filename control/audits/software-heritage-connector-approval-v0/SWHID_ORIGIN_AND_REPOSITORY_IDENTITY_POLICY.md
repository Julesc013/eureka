# SWHID Origin And Repository Identity Policy

SWHID, origin URL, and repository identity review are required. Arbitrary public-query origin or SWHID live fetch, private repository access, credentialed repository access, token-required repository use, local paths, raw origin publication, and uploaded-file identifiers are forbidden now.
