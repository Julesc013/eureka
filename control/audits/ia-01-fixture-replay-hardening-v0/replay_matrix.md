# Replay Matrix

The replay matrix is recorded in
`control/inventory/ia_fixture_replay_matrix.json`.

Every row is local-file-only and forbids live calls, downloads, source-cache
writes, evidence writes, and index mutation.
