# Demo Result

The demo creates a sample hunt, pauses and resumes it, records steering preferences, deactivates one preference, lists command history, and verifies an invalid command is rejected.

The demo records no WorkUnits, source probes, external network calls, model/provider calls, review mutation, public index mutation, master index mutation, or deployment.
