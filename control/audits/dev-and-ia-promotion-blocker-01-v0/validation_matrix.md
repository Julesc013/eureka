# Validation Matrix

See `control/inventory/dev_and_ia_promotion_blocker_validation_matrix.json`.

The final promotion gate remains the full discovery command:

```text
python -m unittest discover -s tests -t .
```
