# Aggregation Eligibility Model

Observations are not public demand until privacy-filtered, poisoning-filtered, and aggregated.
Single observations must not be displayed as public demand.
High privacy risk excludes aggregation.
High poisoning risk excludes aggregation.
Aggregates must use buckets, not raw queries.
Public demand counts remain disabled until a later demand-dashboard runtime plan.
