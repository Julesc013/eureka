# Operations Matrix

Considered hosting modes:

- static snapshot site
- read-only relay service
- local preview server
- future dynamic gateway

Preferred dry-run modes:

- static snapshot site
- read-only relay service

The deploy dry run must verify health checks, smoke checks, rollback checks,
environment requirements, and observability before any future launch approval.
