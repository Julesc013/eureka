# Priority And Scheduling Model

Priority fields are example/future metadata only. P63 examples set
`demand_count_claimed: false` and do not fabricate public demand.

Scheduling fields record future posture such as `future_manual` or
`future_after_approval`. No schedule, worker, retry loop, timeout handler, or
queue processor is created.
