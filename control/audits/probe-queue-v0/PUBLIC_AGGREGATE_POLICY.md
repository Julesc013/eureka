# Public Aggregate Policy

Probe queue aggregate reporting is future-only. Allowed aggregate dimensions may
include probe kind, source family, target kind, priority class, approval
required, operator required, and human required.

Raw query aggregation and private identifier aggregation are false.
