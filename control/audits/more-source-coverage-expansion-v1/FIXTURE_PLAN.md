# Fixture Plan

Internet Archive recorded fixtures:

- Extend the Firefox XP support fixture with a `52.9.0esr` Windows XP compatible installer text stand-in and release-support README.
- Add an old blue FTP client Windows XP candidate item with installer/readme file-list evidence.

Local bundle fixtures:

- Extend the Windows XP browser/tools bundle with Firefox XP and blue FTP installer text members.
- Strengthen the Windows 98 registry repair bundle text evidence around the direct registry-repair installer member.
- Extend the Windows 7 utility bundle with checksum-tool and text-editor app installer text members.

All payloads are tiny text fixtures. `.exe.txt` names are text stand-ins and are not executable software.

