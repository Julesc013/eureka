# Local Quarantine/Staging Model v0

This audit pack records Local Quarantine/Staging Model v0. The milestone is
planning/governance/validation only.

It does not implement staging runtime, create staged state, create
`.eureka-local/`, import packs, mutate local indexes, mutate public search,
upload, submit, call networks, call models, or mutate the master index.
