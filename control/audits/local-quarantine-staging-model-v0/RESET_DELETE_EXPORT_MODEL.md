# Reset, Delete, And Export Model

Future tooling must support:

- list staged packs
- inspect staged pack metadata
- delete one staged pack
- clear all staged state
- export staging report only
- export public-safe contribution candidate, future

Future tooling must not export private staged content by default. Irreversible
mutation requires explicit confirmation. This milestone implements no tooling.
