# F0 Start Policy

F0 decision: `resume_f0`.

Recommended start branch while promotion remains plan-only: `dev`.

If `DEV-TO-MAIN-MERGE-R0` is explicitly applied and pushed first, F0 may start from `main` after validation passes again.

F0 must use the recovered R0 runtime seams and must not reintroduce scaffold-only completion.
