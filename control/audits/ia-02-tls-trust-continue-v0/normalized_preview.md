# Normalized Preview

The live probe produced two redacted normalized preview records:

- `metadata_search_result`
- `item_metadata`

Both records require review and have `accepted_truth: false`. They are source
observation candidate preview material only.

