# Lane Projection Matrix

The kernel emits lane snapshots through the existing Workbench result-lane
projection seam.

Profiles validated:

- `operator_workbench`
- `public_web`
- `native_desktop_read_only`

The IA-HUNT integration is dry-run only in this task. IA WorkUnits and candidate
lanes are planned and projected without live calls or store mutation.
