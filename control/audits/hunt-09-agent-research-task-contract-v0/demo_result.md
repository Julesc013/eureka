# Demo Result

`scripts/demo_agent_research_task.py` creates local hunt context, attaches an exhaustion report, creates a SearchNeed, drafts tasks from hunt and need, and prints the report schema.

The demo verifies provider and execution flags remain false and the local public index summary is unchanged.
