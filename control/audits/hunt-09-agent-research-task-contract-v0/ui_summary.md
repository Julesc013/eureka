# UI Summary

Hunt and SearchNeed detail pages show disabled agent research task drafts, provider-disabled status, candidate-only output semantics, and review-required state.

The UI has draft forms only. It has no provider run, browser, source probe, extraction, or deployment controls.
