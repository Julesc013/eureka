# Provider Disabled Boundary

- Model provider calls are disabled.
- Provider credentials are not accepted.
- Browser/network research calls are disabled.
- The CLI exposes no run command.
- Local service exposes no execution route.
- Execution attempts fail closed through unsupported command or route handling.
