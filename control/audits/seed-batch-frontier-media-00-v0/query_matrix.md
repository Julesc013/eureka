# Query Matrix

The canonical query matrix is
`control/inventory/seed_batch_frontier_media_query_matrix.json`.

It contains 12 curated frontier-resolution media queries and forces the seed
domain to `frontier_resolution_media` even when the generic planner would treat
broad HDTV phrasing as ambiguous.
