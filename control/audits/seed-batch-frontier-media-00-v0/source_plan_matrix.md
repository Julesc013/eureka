# Source Plan Matrix

Archive.org metadata is allowed only as bounded metadata. Wayback/CDX and
Wikidata are descriptor-only in this task. Manual source packs are fixture
replay only. Arbitrary web crawling is disabled.
