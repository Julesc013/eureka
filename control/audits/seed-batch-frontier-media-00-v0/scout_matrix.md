# SCOUT Matrix

SCOUT runs over local candidate memory only. It produces deterministic
relations, discovery trail summaries, related paths, source-trust hints, and
WorkUnit seed hints without live calls.
