# HUNT-01 Search Hunt Session Runtime Audit

This audit pack records the HUNT-01 runtime proof for durable local Search Hunt Sessions.

The task adds a manifest-backed `search_hunt` SQLite store, runtime APIs, CLI, demo, validator, policies, inventories, docs, and focused tests. It does not create WorkUnits, run source probes, call model providers, mutate reviewed indexes, deploy, or claim production/public launch readiness.
