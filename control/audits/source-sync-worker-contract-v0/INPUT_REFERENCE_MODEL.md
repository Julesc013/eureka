# Input Reference Model

Input refs may point to query observations, result cache entries, miss ledger entries, search needs, probe queue items, demand dashboard snapshots, source inventory entries, source packs, or evidence packs. They are references only.
