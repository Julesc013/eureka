# Example Observation Review

Example root:

`examples/query_observations/minimal_query_observation_v0/`

Files:

- `QUERY_OBSERVATION.json`
- `README.md`
- `CHECKSUMS.SHA256`

The example uses synthetic public-safe query terms for `windows 7 apps`.
It is not collected from a real user, retains no raw query, includes no private
path or credential, records summary-only result posture, and keeps all mutation
flags false.
