# Provenance And Source Attribution Review

Each candidate must include source/provenance context or be classified
insufficient or unknown. The dry-run emits warnings or invalid summaries when
source family, source reference, or provenance reference is missing.

The dry-run does not approve sources, prove source trust, or look up source
cache references. It preserves limitations and reports candidate effect classes
only.
