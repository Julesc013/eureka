# Connector Runtime Boundary

The dry-run does not execute connector runtimes, call live sources, execute
source-sync workers, or validate connector health.

It may read synthetic examples representing future connector/evidence outputs.
Connector runtime planning and approval packs remain separate gates.
