# Unresolved Items

No targeted historical drift group remains unresolved.

External full-discovery remains unresolved by design: the previous checkpoint
run was red, and this task did not run full discovery inside the AI session.
One fresh external rerun is now justified after the repair commit is pushed.

Main promotion remains blocked until that returned external rerun reports:

```text
failures: 0
errors: 0
```
