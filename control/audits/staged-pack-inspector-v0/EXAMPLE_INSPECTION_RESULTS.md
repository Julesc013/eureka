# Example Inspection Results

The committed example inspection covers:

- `examples/local_staging_manifests/minimal_local_staging_manifest_v0`
- manifest ID `example.local_staging_manifest.minimal_v0`
- status `planned_example`
- staging mode `stage_local_quarantine`
- two staged pack references
- three staged entities
- one staged issue
- one local-private record
- two public-safe candidate records

The example is synthetic. It is not real staged state and contains no real
private paths, credentials, executable payloads, raw cache/database content, or
user data.
