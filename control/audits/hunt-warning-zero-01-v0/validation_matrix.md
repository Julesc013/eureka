# Validation Matrix

The committed validation matrix is in `control/inventory/hunt_warning_zero_validation_matrix.json`.

AIDE checks, HUNT validators, LOCAL validators, workflow smoke, full unittest discovery, generated artifact cleanliness, architecture boundaries, and runtime leakage gates are recorded there.
