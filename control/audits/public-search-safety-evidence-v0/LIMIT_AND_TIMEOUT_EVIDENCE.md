# Limit And Timeout Evidence

The runner accepted query length exactly at 160, rejected length 161, accepted limit 25, rejected limit 26, and rejected negative/non-integer limits. Timeout behavior is recorded as contract-only because no safe timeout simulation exists in the local in-process harness.
