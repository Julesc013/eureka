# Status Endpoint Evidence

`/healthz`, `/status`, and `/api/v1/status` returned public-safe status with `local_index_only`, `hosted_deployment_verified: false`, and disabled live probes, downloads, uploads, local paths, arbitrary URL fetch, telemetry, and accounts.
