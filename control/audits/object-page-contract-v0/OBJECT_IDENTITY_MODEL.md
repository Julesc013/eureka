# Object Identity Model

`object_identity` records object kind, label, aliases, identifiers, parent/child refs, confidence, status, and `identity_not_truth: true`. P79 examples never claim authoritative object identity.
