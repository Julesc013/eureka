# Hosted Backend Evidence

Backend URL source: unavailable. No hosted backend URL was configured in P77, so no public backend HTTP requests were made. Status: `not_configured`.

Operator action: deploy the hosted public search wrapper separately, keep safe env flags, record the URL, and rerun `python scripts/verify_public_hosted_deployment.py --backend-url <url> --json`.
