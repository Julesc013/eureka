# Public Claim Review

- Static site live claim: false; configured URL returned 404.
- Hosted public search live claim: false; backend URL not configured.
- Production readiness claim: false.
- Live connectors claim: false.
- Downloads, uploads, accounts, telemetry, AI runtime, source cache runtime, evidence ledger runtime, and master-index mutation claims: false.

If static only is later verified, claim only that the static site is live and keep backend unverified. If the backend is later verified, claim hosted public search alpha only as reachable `local_index_only`.
