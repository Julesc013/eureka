# Latency Smoke

LOCAL-10 records elapsed milliseconds for route checks. The latency policy has
no strict performance gate; values are smoke estimates only.

The report includes the slowest local routes to make future regressions visible.
