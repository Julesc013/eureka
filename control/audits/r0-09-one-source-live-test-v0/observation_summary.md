# Observation Summary

The PyPI response was wrapped as a `MetadataResponse`, then converted into a `SourceObservation` and `NormalizedObservation`.

Normalized fields include package name, version, summary, project URLs, release count, and metadata limitations. The observation remains candidate-level metadata and does not assert global source truth.
