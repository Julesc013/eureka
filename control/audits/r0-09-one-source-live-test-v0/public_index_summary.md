# Public Index Summary

The reviewed public index was rebuilt from the explicit source cache, evidence ledger, and review queue databases.

Accepted local review records are included. Rejected, blocked, superseded, queued, and needs-review records remain excluded by the rebuild policy. The rebuild writes only the explicit public index SQLite database.
