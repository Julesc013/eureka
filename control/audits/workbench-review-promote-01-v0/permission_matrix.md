# Permission Matrix

- Operator Workbench: may dry-run, record token-gated decisions, and run explicit temp refresh proof.
- Public Web: read-only and blocked from review/promote decisions.
- Native Read-Only: read-only and blocked from review/promote decisions.

Operator instance mutation remains disabled by default.
