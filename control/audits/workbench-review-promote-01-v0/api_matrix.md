# API Matrix

The API matrix keeps review and promotion actions operator-gated. Public and native projections are read-only. Promotion preview responses describe planned local-reviewed deltas but do not mutate master or committed public index artifacts.
