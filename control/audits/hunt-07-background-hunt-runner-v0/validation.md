# Validation

Primary validation command:

```text
python scripts/validate_background_hunt_runner.py
```

Focused tests cover planning, safe worker running, policy blocking, API routes, workbench UI, auth, and scripts.

