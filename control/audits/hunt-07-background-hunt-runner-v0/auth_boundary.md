# Auth Boundary

Plan preview is read-only. Run controls require an operator token and localhost client scope.

Missing token, invalid token, and LAN requests are rejected before worker dispatch.

Raw tokens are not stored in hunt run records.

