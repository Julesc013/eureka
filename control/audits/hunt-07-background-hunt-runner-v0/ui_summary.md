# UI Summary

The hunt detail page now shows a background hunt runner section with:

- runnable linked WorkUnits,
- policy-blocked linked WorkUnits,
- latest runner history,
- token-gated run-next and run-batch controls,
- disabled future-action boundary notes.

The UI does not add source probe, extraction, AI/model, acquisition, LAN, or deployment controls.

