# Future Runtime Path

Future runtime path after approval:

1. Review sandbox/resource/privacy/executable-payload policy.
2. Keep extraction disabled by default.
3. Build local dry-run normalizers over synthetic fixtures only.
4. Add sandboxed metadata-only prototype over operator-approved local fixtures.
5. Emit reports before any mutation.
6. Add source-cache/evidence-ledger candidate writers only after separate runtime approvals.
7. Keep public search and object pages read-only and cache/index driven.

Next recommended branch: P96 Search Result Explanation Contract v0. Deep Extraction Runtime Planning v0 is deferred until sandbox and payload safety policy approval.
