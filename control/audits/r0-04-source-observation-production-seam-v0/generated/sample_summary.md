# Sample Summary

The generated sample proves the in-memory product seam:

source record -> policy decision -> metadata request -> metadata response -> source observation -> normalized observation -> evidence candidate -> review item

The sample enables no durable writes and no public index writes.
