# Public Aggregate Policy

Public aggregate use is allowed only after privacy filtering. Individual cache
entries are not publishable by default.

Future aggregate reports may count safe cache outcomes by fingerprint, hit
state, gap type, and index snapshot, but they must not expose raw queries, IPs,
accounts, private paths, private URLs, or local result identifiers.
