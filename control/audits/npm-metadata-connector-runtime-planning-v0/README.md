# npm Metadata Connector Runtime Planning v0

P91 is planning-only and approval-gated. It adds no npm metadata connector runtime, no live npm registry calls, no npm/yarn/pnpm CLI calls, no package download or install, no dependency resolution, no npm audit, no lifecycle script execution, no source-cache or evidence-ledger writes, no public-search fanout, no telemetry, no credentials, and no index mutation.

Readiness decision: `blocked_connector_approval_pending` because P75 is present but `connector_approved_now` is false.
