# Package Identity Review And Normalization Plan

Allowed future package-name sources: reviewed source record, reviewed search need, source pack record, manual observation record, and fixture example.

Forbidden package-name sources: raw public query parameter, private package, private scope, credentialed registry, alternate registry unless explicitly approved later, localhost URL, file URL, uploaded file, local path, and arbitrary URL.

Future normalization: npm package-name normalization policy, scoped package parsing, scope-name review, case/punctuation normalization where applicable, Unicode/confusable caution, typo-squatting caution, package rename/reuse caution, private package/scope rejection, credential stripping/rejection, package-name public-safe fingerprint for sensitive refs, and no raw private package/registry publication.

No implementation is added.
