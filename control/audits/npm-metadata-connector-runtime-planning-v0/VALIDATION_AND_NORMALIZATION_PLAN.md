# Validation And Normalization Plan

Future validation covers response shape validation, package-name validation, scoped package validation, package metadata normalization, version normalization, dist-tag normalization, deprecation status normalization, engines metadata normalization, dependency metadata summary without dependency resolution, lifecycle script metadata summary without script execution, tarball metadata summary without tarball download, checksum/digest metadata summary if present as metadata only, license/source terms metadata caution, publisher/maintainer metadata caution, source attribution, error envelope, raw payload exclusion, tarball/package file exclusion, and private package/registry redaction/rejection.

No implementation is added.
