# Validation

Focused validation for `PUBLIC-ALPHA-HOSTING-READINESS-00` is expected to run
through the hosting readiness validator, public alpha read-only validator,
snapshot/source validators, architecture boundary check, generated artifact
cleanliness check, and focused unittest modules.

Full unittest discovery is not run inside the AI session.
