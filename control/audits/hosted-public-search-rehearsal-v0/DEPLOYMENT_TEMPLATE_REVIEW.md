# Deployment Template Review

`Dockerfile` and `deploy/render/render.yaml` are reviewed as templates only. They keep local_index_only mode and disabled live probes/downloads/uploads/local paths/arbitrary URL fetch/install actions/telemetry. They are not deployment evidence.
