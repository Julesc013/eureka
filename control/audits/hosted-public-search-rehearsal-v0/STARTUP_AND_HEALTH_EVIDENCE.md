# Startup And Health Evidence

`run_hosted_public_search.py --check-config` passed under the safe environment, the local wrapper process started on `127.0.0.1`, and `/healthz` returned HTTP 200 with local_index_only and disabled safety flags.
