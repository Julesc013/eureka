# Static Handoff Compatibility

The static search surfaces and `site/dist/data/search_config.json` remain backend-unconfigured, no-JS compatible, free of fake hosted backend claims, and keep hosted form submission disabled until real operator evidence exists.
