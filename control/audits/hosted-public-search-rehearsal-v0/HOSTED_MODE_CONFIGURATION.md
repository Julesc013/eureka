# Hosted Mode Configuration

The rehearsal used safe hosted-public defaults: `EUREKA_PUBLIC_MODE=1`, `EUREKA_SEARCH_MODE=local_index_only`, live probes/downloads/uploads/local paths/arbitrary URL fetch/install actions/telemetry disabled, max query length 160, max results 20, and timeout budget 5000 ms.
