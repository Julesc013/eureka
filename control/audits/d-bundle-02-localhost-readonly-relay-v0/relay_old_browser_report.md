# Relay Old Browser Report

The lite HTML profile uses no required JavaScript, no fetch/XHR, no login, no
cookies, no modern CSS requirement, and no external image dependency.

The old-browser output is a fixture projection only and does not host or deploy
public pages.

