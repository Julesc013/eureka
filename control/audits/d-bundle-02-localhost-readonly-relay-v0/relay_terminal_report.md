# Relay Terminal Report

Terminal mode provides plain text, numbered menus, and record summaries with the
same semantic fields as HTML/text renderers.

Terminal mode does not execute commands, download files, upload data, sync
sources, authenticate accounts, or emit telemetry.

