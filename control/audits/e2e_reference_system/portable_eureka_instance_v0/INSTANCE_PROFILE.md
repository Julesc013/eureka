# Instance Profile

Portable profile path:

```text
<instance>/config/portable_instance.json
```

Schema version: `eureka.portable_instance.v0`

The profile records instance identity, schema version, default exploration host/port, disabled public/live/download/execution/telemetry/cloud flags, and instance-relative state roots. It stores no token or credential.
