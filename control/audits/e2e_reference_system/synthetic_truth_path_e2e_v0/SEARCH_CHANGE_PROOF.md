# Search Change Proof

The isolated synthetic index starts with one candidate document. After synthetic materialization, a second generation adds the reviewed synthetic document and local lexical ranking returns it above the candidate. Rollback restores the active pointer to the baseline generation.

