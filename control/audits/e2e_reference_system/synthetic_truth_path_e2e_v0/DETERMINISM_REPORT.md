# Determinism Report

Semantic IDs, reviewed-record ID, generation IDs, index hash, and snapshot manifest ID are deterministic across clean reruns. Canonical Review Ledger decision and event IDs are append-only audit IDs generated by the existing ledger implementation; the scenario records that posture rather than rewriting ledger behavior globally.

