# Parity Model

Parity cases compare semantic fingerprints, not byte-identical renderer output.

Covered projections:

- JSON v0;
- text v0;
- HTML-basic v0;
- snapshot v0;
- exploration HTML/API.

Required parity:

- equivalent status;
- equivalent authority;
- no representation invents verified truth;
- policy and boundary posture remain visible.
