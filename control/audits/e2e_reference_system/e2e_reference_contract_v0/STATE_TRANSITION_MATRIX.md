# State Transition Matrix

See `docs/reference/E2E_REFERENCE_STATE_TRANSITIONS.md`.

The matrix is contract and architecture authority for the next runner task. It
does not implement runtime transition logic.

