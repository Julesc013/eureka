# Semantic Authority Matrix

The matrix selects existing authority where possible and records profile-only
gaps where the next runner task needs shared meaning but not a new schema.

See `semantic_authority_matrix.json` and
`docs/reference/E2E_REFERENCE_CONTRACT_MAP.md`.

