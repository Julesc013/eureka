# Replay Serialization Profile

See `docs/reference/E2E_REFERENCE_REPLAY_PROFILE.md`.

The profile reuses `contracts/resolution/run/run_event.v0.json` where possible
and adds E2E runner expectations for sequence, payload hashes, causation,
correlation, privacy, authority, and synthetic isolation.

