# Accessibility Report

Explore pages are server-rendered HTML with document title, language marker, main landmark, navigation, labelled forms, headings, tables, and no required JavaScript.

The existing local Workbench page validator is run over generated Explore HTML in focused route tests.

