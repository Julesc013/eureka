# Run Control Model

Synthetic Hunts complete synchronously through the shared E2E Reference Runner.

The UI renders pause, resume, cancel, step, and replay controls. Terminal runs disable pause, resume, cancel, and step. Replay remains available and validates the durable bundle.

All POST controls are loopback-only and operator-token gated.

