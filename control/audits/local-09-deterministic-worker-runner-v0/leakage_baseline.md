# Leakage Baseline

Runtime leakage gate status before LOCAL-09: fail.

Runtime leakage gate status after LOCAL-09: fail.

New unallowlisted production findings before: 1030.

New unallowlisted production findings after: 1030.

LOCAL-09 increased leakage: false.

The failure is the known pre-existing runtime leakage gate. `runtime/local/worker` contains no new unallowlisted production-path leakage findings.
