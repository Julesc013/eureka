# Issue Model

Issues have severity `info`, `warning`, `error`, or `blocked`.

Issue types include schema errors, checksum errors, privacy errors, rights or
risk review requirements, executable payload detection, raw database detection,
private path detection, credential detection, unknown pack types, unavailable
validators, unsupported features, and policy blocks.

Error and blocked issues require remediation. Private paths must be redacted in
shareable reports.
