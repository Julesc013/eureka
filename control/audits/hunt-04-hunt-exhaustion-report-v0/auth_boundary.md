# Auth Boundary

Report reads are open to local read routes. Report generation is a localhost-only POST operation and requires an operator token. Missing and invalid tokens are rejected. LAN clients cannot generate or update reports.
