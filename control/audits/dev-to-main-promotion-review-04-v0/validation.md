# Validation

This task uses focused validators plus the external full-discovery gate result.

The expected refusal-path trace is nonblocking because full discovery passed
with exit code 0, failures 0, and errors 0.

Full unittest discovery was not run inside AI.
