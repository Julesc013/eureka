
# Next Task Decision

Recommended next task: `SOURCE-SNAPSHOT-BASELINE-CLOSEOUT-01-RESUME`.

Run `python scripts/run_full_unittest_discovery.py --out ../eureka-test-runs/source_snapshot_closeout` outside AI and return only the compact artifacts listed in `external_full_discovery_handoff.json`.

`DEV-TO-MAIN-PROMOTION-REVIEW-03` and `PUBLIC-ALPHA-READONLY-00` are blocked until external full discovery is green or an explicit approved split is recorded.
