# Behavior Test Summary

Tests cover initialization, idempotent migration, in-memory and file-backed
stores, review item persistence, event ordering, evidence links, source-cache
links, explicit decisions, filtering, summaries, integrity checks, and runtime
dependency boundaries.
