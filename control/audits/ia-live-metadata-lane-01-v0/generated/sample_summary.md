# IA Live Metadata Lane Summary

The IA live metadata lane is present in Workbench resolution runs as a
policy-gated candidate lane. It is blocked by default, can be exercised through
dry-run or deterministic mock-live paths, and preserves all no-write/no-download
boundaries.
