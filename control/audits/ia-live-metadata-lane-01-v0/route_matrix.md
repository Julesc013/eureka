# Route Matrix

See `control/inventory/ia_live_metadata_lane_route_matrix.json`.

The existing Workbench resolution-run routes expose the IA metadata lane through
run packets, lane snapshots, event logs, and command responses.
