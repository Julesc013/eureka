# Command Matrix

See `control/inventory/ia_live_metadata_lane_command_matrix.json`.

Operator projection can run dry-run and mock-live paths. Real live execution is
blocked unless `allow_live`, operator token, and policy gates are supplied.
Public and native read-only projections are blocked.
