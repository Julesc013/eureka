# Reviewed Record Schema

Schema inventory: `control/inventory/ia_reviewed_record_schema.json`.

The reviewed local record preserves promotion preview ID, review decision ID,
candidate ID, evidence IDs, source-cache IDs, observation IDs, source locator,
provenance, uncertainty, limitations, rights flags, and risk flags.

It is a local reviewed projection only: not a master record and not a hosted
public record.
