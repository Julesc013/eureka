# Fixture Rebuild Result

Fixture promotion previews produced reviewed local records and were written only
to the temporary explicit instance reviewed local index.

No operator instance, master index, hosted/public index, or committed
`site/dist/data/public_index` path was mutated.
