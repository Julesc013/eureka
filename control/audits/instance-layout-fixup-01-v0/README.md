# INSTANCE-LAYOUT-FIXUP-01

This audit records the fixup review after `INSTANCE-LAYOUT-01`.

The commit `ff484007` was already present at `origin/dev`, so its message could
not be amended without a forbidden history rewrite or force push. The full-suite
failures were classified from the original full discovery output and a targeted
rerun after the tree was clean.
