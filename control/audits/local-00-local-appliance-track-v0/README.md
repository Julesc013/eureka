# LOCAL-00 Local Appliance Track

LOCAL-00 forks the queue from F0 into a Local Appliance track. It is a planning and control task only.

The audit records that `main` and `dev` were aligned before edits, F0 remains resumable but deferred, and the next current task is `LOCAL-01`. No server, HTML workbench, WorkUnit runtime, LAN binding, deployment, production readiness claim, or public launch claim is introduced here.
