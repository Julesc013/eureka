# SearchNeed Runtime Summary

HUNT-05 adds `runtime/search/need`, a durable SQLite-backed local store under `db/search_need.sqlite`.

The store supports create/list/show, creation from Search Hunt, state transitions, transition history, hunt links, exhaustion links, summaries, and integrity checks.
