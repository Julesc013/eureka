# State Machine

SearchNeed states are `proposed`, `open`, `waiting_for_user`, `waiting_for_policy`, `blocked`, `satisfied_locally`, `superseded`, and `cancelled`.

Invalid transitions fail closed and transitions are recorded.
