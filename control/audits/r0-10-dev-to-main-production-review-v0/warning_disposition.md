# Warning Disposition

Warnings are classified in `control/inventory/r0_warning_disposition.json`.

Non-blocking warnings include historical R0 `pass_with_warnings` statuses and legacy architecture leakage allowlist entries.

Warnings assigned to remediation:

- known allowlisted legacy architecture leakage
- contract taxonomy compatibility shims
- R0-03B-2 contract taxonomy warning
