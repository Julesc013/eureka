# Eval And Regression Plan

Future gates: archive resolution evals must stay satisfied; search usefulness audit must not regress without explanation; manual external baseline comparison should inform ranking priorities; ranking regression corpus is required before runtime enablement; regression tests must compare old order versus proposed order; result suppression must be impossible; conflicts and gaps must remain visible; deterministic tie-break is required; fixture-backed evals are not real-world proof; production ranking requires external baseline evidence and hosted deployment evidence.

Explicit gate phrase: ranking regression corpus required.
