# Connector Scope

Scope is npm package metadata-only: package, version, dist-tag, tarball metadata summary, engines, license, dependency metadata summary, deprecation-like status, lifecycle script metadata summary, and publisher/maintainer metadata may be considered only after approval.
