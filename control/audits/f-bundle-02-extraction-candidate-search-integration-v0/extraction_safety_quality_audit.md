# Extraction Safety Quality Audit

The integration layer reads committed extraction result JSON only. It does not inspect private files, download files, execute payloads, run installers, scan for malware, mutate stores, or change public search behavior.
