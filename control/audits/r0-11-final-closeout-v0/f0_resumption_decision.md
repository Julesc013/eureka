# F0 Resumption Decision

Decision: `remediation_required`.

F0 must remain blocked until the contract taxonomy blocker is resolved. When F0 resumes, it must use recovered runtime seams and must not recreate scaffold-only H-series patterns.
