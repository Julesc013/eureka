# Final Blocker Register

The machine-readable blocker register is stored in `control/inventory/r0_final_blocker_register.json`.

Remaining blocker:

- Contract taxonomy is not clean enough for F0 or branch promotion.

Child task:

- `R0-REMEDIATION-CONTRACT-TAXONOMY-01`
