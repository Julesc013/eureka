# Side Effect Boundary

Allowed side effects are local preflight and gate record writes. Disabled side effects include provider/model use, source probes, extraction, review changes, public/master index changes, site writes, and deployment.
