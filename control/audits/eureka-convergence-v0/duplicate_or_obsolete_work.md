# Duplicate Or Obsolete Work

## Do Not Re-run As-is

- `EUREKA-AIDE-REAL-02` and `EUREKA-AIDE-REAL-03`: merge their intent into the
  track spine. Deterministic eval/report/check improvements should be scoped to
  Track A/B/D/C/E tasks rather than generic AIDE real-work placeholders.
- Old public-alpha prompts that imply immediate hosting: obsolete as direct
  Codex tasks. Actual hosted public alpha is Track E only.
- P77-style hosted deployment evidence prompts: operator-gated; do not rerun
  locally as a deployment claim.
- P78 external baseline comparison: requires Manual Observation Batch 0 data
  first. Without observations it remains no-observations readiness only.

## Merge Into Track A

- Object, source, comparison, identity, merge/deduplication, evidence-weighted
  ranking, compatibility-aware ranking, search-result explanation, public result
  card, and public data projection work should converge into representation and
  view-model tasks.

## Merge Into Track B

- Query observation, shared cache, miss ledger, search need, probe queue,
  candidate index, candidate promotion, source sync, source cache, evidence
  ledger, connector approval, connector runtime planning, pack import, staging,
  and dry-run runtime work should converge into the node/contribution/source/
  evidence/review loop.

## Merge Into Track D

- Snapshot format, snapshot consumer, static/public data bundles, relay surface
  design, and relay prototype planning should converge before native work.

## Defer To Track C

- Visual Studio/Xcode projects, native GUI, FFI, installers, native cache
  runtime, downloads, local archive scanning, relay sidecars, and native client
  packaging remain deferred until Track C approval.

## Defer To Track E

- Hosted backend URL configuration, GitHub Pages success claims, DNS/TLS,
  rate limits, edge controls, monitoring, logs, rollback, live backend
  deployment, and public hosted alpha claims remain Track E.
