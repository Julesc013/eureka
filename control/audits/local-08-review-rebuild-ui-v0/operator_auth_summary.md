# Operator Auth Summary

Mutating review and rebuild routes require an operator token. Tokens may be
provided to the local server in memory or stored as a salted hash under the
explicit local instance config directory. Raw tokens are not persisted or
printed by the token setup script.
