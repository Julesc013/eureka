# Fingerprint Matrix

Fingerprints are deterministic over normalized title, source family, source
locator, domain id, object hint, version hint, platform hint, and checksum hint.
Collisions require review.
