# PUBLIC-ALPHA-READONLY-00

This audit records the reviewed-snapshot-only public alpha foundation.

The alpha routes are local runtime routes only. They are read-only, snapshot
backed, relay backed, and explicitly do not perform deployment, live source
fanout, downloads, extraction, model/provider calls, or public launch claims.
