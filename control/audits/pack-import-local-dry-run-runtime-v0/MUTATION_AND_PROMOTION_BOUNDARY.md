# Mutation And Promotion Boundary

The dry-run may produce reports only.

It does not perform authoritative import, source-cache writes, evidence-ledger
writes, candidate-index writes, public-index rebuilds, local-index mutation,
master-index mutation, promotion decisions, or accepted record creation.

Promotion runtime remains separate, explicit, review-gated, and
operator-approved.
