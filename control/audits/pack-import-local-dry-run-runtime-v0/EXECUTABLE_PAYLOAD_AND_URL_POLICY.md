# Executable Payload And URL Policy

P104 allows no execution, scripts, installers, package manager invocation,
emulator launch, VM launch, URL following, downloads, file retrieval, or archive
deep extraction.

Executable references require risk labels and review. URL references are
metadata only and must not be fetched. The runtime makes no malware safety
claim.
