# Validation And Normalization Plan

Future runtime must define:

- response shape validation
- package-name validation
- project metadata normalization
- release version normalization
- release date normalization
- file metadata summary
- yanked status normalization
- requires_python normalization
- classifier normalization
- dependency metadata summary without dependency resolution
- checksum/digest metadata summary if present as metadata only
- license/source terms metadata caution
- source attribution
- error envelope
- raw payload exclusion
- wheel/sdist/package file exclusion
- private package/index redaction or rejection

No implementation is added in P90.
