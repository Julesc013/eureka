# Sample Summary

The preferred local development instance is `../instances/default`.

The legacy sibling `../eureka-instance` remains available only when supplied
explicitly. Repository scripts do not move, delete, or copy that operator-owned
state unless a future explicit operator command chooses to do so.
