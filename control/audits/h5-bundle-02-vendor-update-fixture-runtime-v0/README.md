# Readme

H5-BUNDLE-02 adds fixture-only vendor/update/driver normalizers. It enables no live access, catalog fetching, downloads, vendor tools, package managers, installer execution, firmware flashing, install, execution, or truth acceptance.
