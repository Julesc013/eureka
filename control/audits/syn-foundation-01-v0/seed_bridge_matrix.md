# Seed Bridge Matrix

Every synthetic query case maps to:

- one review-gated SearchNeed seed proposal
- one review-gated, blocked, or dry-run-only WorkUnit seed proposal

The seed bridge does not create runtime SearchNeeds or WorkUnits. It does not execute work, approve sources, accept evidence, or mutate indexes.
