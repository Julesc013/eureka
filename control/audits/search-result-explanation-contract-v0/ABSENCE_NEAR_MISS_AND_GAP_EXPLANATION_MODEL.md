# Absence Near Miss And Gap Explanation Model

Absence is scoped, never global. Near misses and gaps include source coverage gaps, capability gaps, compatibility evidence gaps, member access gaps, representation gaps, query interpretation gaps, live probe disabled, external baseline pending, deep extraction missing, OCR missing, source cache missing, and evidence ledger missing.
