# Query Interpretation Explanation Model

Raw query text is not included by default. The model includes normalized public-safe query text, interpreted intent, extracted and redacted hints, confidence, interpretation_not_truth true, and limitations. Private query fragments must not be exposed.
