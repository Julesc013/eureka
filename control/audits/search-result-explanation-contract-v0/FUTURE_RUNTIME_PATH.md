# Future Runtime Path

Future runtime planning must prove public search safety, public index compatibility, source/evidence boundaries, no hidden scores, no suppression, no model calls by default, no raw private query exposure, and operator approval before any local or hosted explanation runtime.
