# Next Steps

Recommended next branch: P85 Compatibility-Aware Ranking Contract v0.

Do next:

- Define compatibility-aware ranking as another contract-only lane.
- Keep ranking runtime, result ordering changes, suppression, candidate promotion, and index/cache/ledger mutation disabled until future runtime planning and eval evidence exist.
- Continue human/operator parallel work: deploy hosted wrapper, configure backend URL, configure edge/rate limits, verify static site, and execute Manual Observation Batch 0.
