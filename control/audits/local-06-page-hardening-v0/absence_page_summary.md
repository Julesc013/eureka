# Absence Page Summary

The absence page now shows the query, local result count, checked local layers, checked source references, unchecked/deferred layers, limitations, and unavailable capabilities.

It states that absence is not proof the artifact does not exist.
