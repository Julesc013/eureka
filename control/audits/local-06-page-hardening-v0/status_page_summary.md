# Status Page Summary

The status page now shows instance ID, instance schema version, display-safe instance root, store manifest count, source cache, evidence ledger, review queue, public index status, migration state, reviewed public index summary, and disabled runtime flags.

It remains local appliance status only, not deployment or launch readiness.
