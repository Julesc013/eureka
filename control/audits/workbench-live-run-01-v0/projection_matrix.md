# Projection Matrix

- `operator_workbench`: full local operator packet, still read-only
- `public_web`: summary with operator-only fields hidden
- `native_desktop_read_only`: read-only summary with operator-only fields hidden

The surface owns rendering only.
