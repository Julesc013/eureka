# Route Matrix

The local Workbench exposes `/runs`, `/runs/{run_id}`, and read-only `/api/v1/resolution-runs*` endpoints. `/search` now links a query to a projected dry-run when a query is present.

All routes are GET-only in this foundation task.
