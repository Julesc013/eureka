# Source Attribution And Policy Review

Each dry-run candidate includes a source family and source reference. Source
policy status is metadata only: `approval_required` remains a gate, and
`approved_example` means only that the committed synthetic example may be read
by the dry-run.

The dry-run does not approve connectors, prove source availability, prove source
trust, or accept source-derived metadata as truth.
