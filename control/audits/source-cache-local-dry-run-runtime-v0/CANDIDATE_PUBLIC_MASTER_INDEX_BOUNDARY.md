# Candidate/Public/Master Index Boundary

The dry-run does not create candidate index records, promote candidates, rebuild
the public index, mutate the local index, mutate the runtime index, or mutate
the master index.

It reports possible effect classes only, such as "evidence review required" or
"no index effect now."
