# Baseline Schema Review

Schema path: `evals/search_usefulness/external_baselines/observation.schema.json`.

Validator command: `python scripts/validate_external_baseline_observations.py`.

The validator reports the current baseline area as valid. The current records are pending-slot manifests and pending Batch 0 slots, not observed external baseline records.
