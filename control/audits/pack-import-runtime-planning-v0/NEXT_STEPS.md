# Next Steps

Recommended next branch: P95 Deep Extraction Contract v0.

Human/operator parallel:

- deploy hosted wrapper
- configure backend URL
- configure edge/rate limits
- verify static site
- Manual Observation Batch 0 Execution
- review pack import operator policy

Do not proceed to pack import local dry-run runtime until operator approval, path policy, executable payload policy, rollback/audit policy, and mutation/promotion gates are explicitly accepted.
