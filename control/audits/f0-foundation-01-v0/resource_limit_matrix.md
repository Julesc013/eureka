# Resource Limit Matrix

Initial F0 limits are 1 MiB fixture files, 100 members, 10 MiB declared/uncompressed totals, max depth 1, filename length 240, 10 second timeout posture, and 64 MiB memory budget.

The limits are for fixture-only validation and do not constitute production extraction readiness.
