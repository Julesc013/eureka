# Member Manifest Matrix

F0 member manifests are manifest-only packets. Member records include normalized path safety, blocked state, declared size, path traversal/absolute path flags, symlink/device flags, and block reasons.

No member contents are read or extracted to the filesystem.
