# Contract Matrix

F0 contracts live under `contracts/extraction/` and define container descriptors, member manifests, member records, member observation candidates, extraction WorkUnit seed suggestions, risk reports, boundary reports, fixture manifests, and read-only console views.

These contracts are candidate-generation and review-boundary contracts only. They do not enable runtime extraction or accepted truth.
