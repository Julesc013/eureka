# Supported Container Matrix

Supported now:

- `zip_fixture_manifest`: tiny committed fixture ZIPs only.
- `directory_manifest_fixture`: descriptor-only examples only.

TAR and broader container support are deferred until future reviewed policy work.
