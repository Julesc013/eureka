# Allowed Metadata Capabilities

Future-only capabilities: URL availability metadata, CDX capture metadata, Memento TimeMap/TimeGate metadata, capture timestamp summary, status/MIME/digest summary, and nearest-capture metadata. None are implemented now.
