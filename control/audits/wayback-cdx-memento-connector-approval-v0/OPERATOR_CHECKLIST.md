# Operator Checklist

Required later and pending: configure User-Agent, contact policy, timeout, rate limit, retry/backoff, circuit breaker, cache destination, evidence destination, URI privacy filter, future connector flag, connector health monitoring, and approval evidence record.
