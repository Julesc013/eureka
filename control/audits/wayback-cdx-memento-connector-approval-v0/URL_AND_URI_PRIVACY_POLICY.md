# URL and URI Privacy Policy

URI-R review is required. Private, credentialed, local, localhost, file, data, and javascript URLs are forbidden. Raw URL publication is forbidden. The synthetic example URI uses example.invalid and is never fetched.
