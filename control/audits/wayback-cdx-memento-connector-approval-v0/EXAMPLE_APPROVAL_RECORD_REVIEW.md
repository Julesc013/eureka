# Example Approval Record Review

The example is synthetic, uses a non-resolving example.invalid URI, leaves checklists pending, and keeps all runtime, external-call, archive-content, WARC, download, fanout, and mutation fields false.
