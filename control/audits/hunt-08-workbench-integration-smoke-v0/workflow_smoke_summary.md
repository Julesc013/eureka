# Workflow Smoke Summary

The smoke path creates a local Search Hunt, applies pause/resume and steering, generates an exhaustion report, creates a linked SearchNeed, persists linked WorkUnits, and runs one safe deterministic local worker.

Policy-blocked source-probe, extraction, and AI/model worker records remain blocked.
