# Future Runtime Path

Future runtime work may add reviewed storage only after separate contracts for
privacy/poisoning, probe queue, candidate index, promotion policy, retention,
deletion, and operator controls.

The public search runtime remains unwired for P62. Public queries do not write
search needs, enqueue probes, create candidates, or update the master index.
