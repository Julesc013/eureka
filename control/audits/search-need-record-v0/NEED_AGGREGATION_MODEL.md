# Need Aggregation Model

P62 examples use `single_example` aggregation with occurrence count 1. This is a
synthetic example count, not public demand.

Future aggregation may group privacy-filtered need fingerprints, object kinds,
platforms, artifact types, source families, gap types, and priority classes.
Raw query aggregation and private identifier aggregation remain false.
