# Public Aggregate Policy

Public aggregate use is future-only and allowed only after privacy filtering
and poisoning review. Allowed future aggregate fields are need fingerprint,
object kind, platform, artifact type, source family, gap type, and priority
class.

Individual P62 records are not publishable by default. Raw query aggregation
and private identifier aggregation are false.
