# Need Identity And Fingerprint Model

Need identity uses a public-safe canonical label, normalized terms,
disambiguation terms, equivalence keys, aliases, and a non-reversible SHA-256
fingerprint.

The fingerprint basis must not include raw private query text, IP addresses,
account IDs, private paths, sensitive tokens, private URLs, or local result IDs.
No secret salt is stored in examples.
