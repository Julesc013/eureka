# Privacy And Redaction Policy

Raw query retention defaults to none. Public-safe search needs must not include
raw private queries, IP addresses, account IDs, private paths, sensitive tokens,
private URLs, user identifiers, local result IDs, executable payloads, or raw
copyrighted payload dumps.

Unsafe material must be rejected or redacted before future aggregation. P62
implements validation only, not runtime collection.
