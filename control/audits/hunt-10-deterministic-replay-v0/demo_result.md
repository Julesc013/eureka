# Demo Result

`scripts/demo_hunt_replay.py` initializes a local hunt workflow in an explicit instance, builds a replay fixture, runs plan-only, runs replay-local, runs verify-existing, and checks that source probe, extraction, and AI/model steps remain blocked.
