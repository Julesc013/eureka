# Auth Boundary

Plan-only and verify-existing modes are read-only. Replay-local requires an operator token and loopback client scope. LAN clients receive a forbidden response on replay-run routes.

Raw operator tokens are not stored or emitted by the replay runtime.
