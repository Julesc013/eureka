# UI Summary

The hunt detail page shows deterministic replay state, the replay plan preview, blocked replay steps, latest diff summary, replay history, and localhost/token-gated replay controls when mutation is available. The UI states that replay is not truth and that future action categories are not replayed.
