# Scoped Absence Policy

Scoped absence means no verified result was found in the checked scope only.

P61 miss entries must keep `global_absence_claimed` false in both
`miss_classification` and `absence_summary`. The entry must name what was
checked, what was not checked, and why the unsearched scope remains outside the
claim.

This preserves the doctrine that a miss is not global truth.

