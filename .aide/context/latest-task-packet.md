# AIDE Latest Task Packet

## PHASE

EUREKA-AIDE-SELFTEST-01 - Repair imported AIDE Lite selftest fixture fallback

## GOAL

Make the imported Eureka-local AIDE Lite `test` and `selftest` aliases pass
without copying optional source `core/**` roots, mutating the AIDE source repo,
or changing Eureka product code.

## WHY

Q26 refreshed Eureka from the repaired Q25 source pack and confirmed the main
AIDE Lite workflow works: doctor, validate, snapshot, index, context, pack,
review-pack, ledger, eval, route explain, and adapter validate. The remaining
local reliability gap is that `py -3 .aide/scripts/aide_lite.py test` and
`selftest` still fail inside their temporary fixture because target-safe import
does not copy optional `core/gateway/**` and `core/providers/**` modules, while
the fixture fallback writes invalid Python placeholder text for those optional
module files. Fixing this is small, measurable, and keeps AIDE Lite usable in
Eureka without broad source-root import.

## CONTEXT_REFS

- `.aide/queue/EUREKA-AIDE-HANDOVER-01/evidence/validation.md`
- `.aide/queue/EUREKA-AIDE-HANDOVER-01/evidence/pack-refresh.md`
- `.aide/queue/EUREKA-AIDE-HANDOVER-01/evidence/quality-readiness.md`
- `.aide/queue/EUREKA-AIDE-HANDOVER-01/evidence/remaining-risks.md`
- `.aide/scripts/aide_lite.py`
- `.aide/scripts/tests/test_aide_lite.py`
- `.aide/scripts/tests/test_gateway_commands.py`
- `.aide/scripts/tests/test_provider_adapter.py`
- `.aide/scripts/tests/test_export_import.py`
- `.aide/policies/export-import.yaml`
- `.aide/context/repo-snapshot.json`
- `.aide/context/repo-map.json`
- `.aide/context/repo-map.md`
- `.aide/context/test-map.json`
- `.aide/context/context-index.json`
- `.aide/context/latest-context-packet.md`
- `AGENTS.md`

## ALLOWED_PATHS

- `.aide/scripts/aide_lite.py`
- `.aide/scripts/tests/test_aide_lite.py`
- `.aide/scripts/tests/test_gateway_commands.py`
- `.aide/scripts/tests/test_provider_adapter.py`
- `.aide/scripts/tests/test_export_import.py`
- `.aide/queue/EUREKA-AIDE-SELFTEST-01/**`
- `.aide/context/**` generated by validation
- `.aide/reports/**` generated by validation
- `.aide/evals/runs/**` generated by validation

## FORBIDDEN_PATHS

- Eureka product source paths: `runtime/**`, `contracts/**`, `surfaces/**`,
  `site/**`, `crates/**`, `native/**`, `tests/**` outside `.aide/scripts/tests/`
- Broad optional source roots: `core/**`, `docs/reference/**` copied from AIDE
- `.git/**`
- `.env`, `.env.*`
- `secrets/**`
- `.aide.local/**`
- AIDE source repo files outside the Eureka checkout
- Dominium files
- Raw provider credentials, API keys, raw prompt logs, raw response logs, local
  cache payloads, live provider/model/network-call code

## IMPLEMENTATION

- Reproduce the failure with `py -3 .aide/scripts/aide_lite.py test` or
  `selftest` and keep the traceback in task evidence.
- Inspect `_write_minimal_repo`, Q19/Q20 required-file fallback behavior, and
  optional Gateway/provider status checks.
- Change only the target-local AIDE Lite selftest fixture/fallback logic so
  missing optional `.py` files are valid inert Python modules in the temp repo.
- Preserve safe importer scope: do not import or create `core/**` in Eureka.
- Add or update focused `.aide/scripts/tests/**` coverage so a missing optional
  Gateway/provider source file no longer creates invalid Python in the
  selftest fixture.
- Keep the fix offline and deterministic. No provider/model/network calls.

## VALIDATION

- `py -3 .aide/scripts/aide_lite.py doctor`
- `py -3 .aide/scripts/aide_lite.py validate`
- `py -3 .aide/scripts/aide_lite.py test`
- `py -3 .aide/scripts/aide_lite.py selftest`
- `py -3 .aide/scripts/aide_lite.py eval run`
- `py -3 .aide/scripts/aide_lite.py verify`
- `py -3 .aide/scripts/aide_lite.py review-pack`
- `py -3 .aide/scripts/aide_lite.py ledger scan`
- `py -3 scripts/check_architecture_boundaries.py`
- `git diff --check`

## EVIDENCE

- Create `.aide/queue/EUREKA-AIDE-SELFTEST-01/` with task/status/plan/prompt
  and evidence files.
- Record the pre-fix traceback, changed files, validation commands/results,
  token estimate for the final packet, verifier/review/eval status, and
  remaining limitations.
- Confirm `.aide.local/` remains ignored and uncommitted.
- Confirm no `core/**` files are copied into Eureka and no source AIDE state is
  imported.

## NON_GOALS

- No Eureka product feature changes.
- No Gateway/provider implementation beyond inert temp-fixture placeholders
  used only by AIDE Lite selftest.
- No source AIDE repo mutation.
- No broad pack import, `--mode full`, Dominium import, live model routing,
  provider calls, local model setup, exact tokenizer, provider billing ledger,
  autonomous loop, semantic cache, vector DB, UI, host/app work, MCP/A2A, or
  automatic GPT review.

## ACCEPTANCE

- `py -3 .aide/scripts/aide_lite.py test` exits 0 in Eureka.
- `py -3 .aide/scripts/aide_lite.py selftest` exits 0 in Eureka.
- The fix is limited to `.aide/**` allowed paths.
- No `core/**` or source `docs/reference/**` files are added to Eureka.
- Safe import mode still skips broad roots and remains conflict-preserving.
- Doctor, validate, eval, verify/review, ledger, architecture boundary check,
  and `git diff --check` are run and recorded.
- No secrets, raw prompt logs, raw responses, `.aide.local/`, provider keys, or
  product source files are committed.

## OUTPUT_SCHEMA

Return a compact final report with `STATUS`, `SUMMARY`, `COMMITS`,
`CHANGED_FILES`, `VALIDATION`, `TOKEN_RESULT`, `QUALITY_RESULT`, `RISKS`, and
`NEXT`. Include exact command outcomes for `test` and `selftest`.

## TOKEN_ESTIMATE

- method: chars / 4, rounded up
- chars: 5767
- approx_tokens: 1442
- budget_status: within_budget
- formal ledger: `.aide/reports/token-ledger.jsonl`
