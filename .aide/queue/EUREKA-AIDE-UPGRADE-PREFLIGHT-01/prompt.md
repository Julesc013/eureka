# Q54 Compact Prompt

Perform an evidence-only preflight inside `julesc013/eureka` before upgrading the target-local AIDE Lite install from the latest stable local release bundle.

Do not install, upgrade, repair, roll back, uninstall, copy bundle files into `.aide/`, push, merge, rebase, mutate branches, call network/providers/models, or edit product/source roots.

Required inspection:

- repo identity and git state;
- existing AIDE version, capabilities, queue, memory, packets, reports, policies, and golden tasks;
- target-local fixes from the prior Eureka AIDE phases;
- Eureka product boundaries and source/evidence/index systems;
- existing validators, scripts, test lanes, repo-health reports, and architecture checks;
- `.aide.local/` ignore/untracked status;
- local AIDE release bundle existence, checksums, manifest, install notes, archive listing, and forbidden-path scan;
- Q55 preservation, conflict, absorption, and validation rules.

Write only the Q54 queue evidence and compact preflight reports. End status must be `needs_review`.
