id: H6-BUNDLE-03
title: Web archive, news, and event approved metadata-only live probes
status: queued
purpose: Prepare approval-gated metadata-only live probe envelopes for H6 web archive, news, event, and public trace metadata after fixture normalizers pass.
allowed_scope_summary: H6 live-probe request manifests, fail-closed wrappers, policies, docs, scripts, tests, audit evidence, and AIDE metadata only.
gate: H6-BUNDLE-02 passes; no live probe may run without explicit committed source approval, and CDX/Memento/WARC/page/media/document fetch, scraping, crawling, bypass, source sync, index mutation, and truth acceptance remain forbidden by default.
recommended_after: H6-BUNDLE-02
