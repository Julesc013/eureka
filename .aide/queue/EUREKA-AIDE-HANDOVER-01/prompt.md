# Compact Prompt

Implement Q26 in `julesc013/eureka` as an AIDE Lite handover review.

Review the existing Q22 Eureka AIDE import evidence, verify that target-local
`.aide/` contains no source AIDE queue/history/memory/generated context/local
state/secrets, check the repaired Q25 source pack if available, refresh only
safe portable AIDE Lite files, regenerate Eureka-local compact packets, confirm
token savings by chars/4 approximation, and produce the first real bounded
Eureka task handoff.

Do not implement Eureka product features, do not mutate the AIDE repo or
Dominium, do not create `.aide.local/`, and do not make provider/model/network
calls.
