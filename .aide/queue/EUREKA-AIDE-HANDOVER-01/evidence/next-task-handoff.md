# Next Task Handoff

The first real bounded Eureka task will be selected after the Q25 refresh and
fresh context generation.

Candidate class under review:

- Create Eureka-specific AIDE Lite golden tasks for compact packet quality and
  target readiness, because Q22 and Q26 both identify the absence of
  Eureka-specific golden tasks as a remaining readiness gap.

The final handoff will include title, objective, rationale, context refs,
allowed paths, forbidden paths, validation commands, acceptance criteria, output
schema, evidence requirements, token estimate, and review-packet guidance.
