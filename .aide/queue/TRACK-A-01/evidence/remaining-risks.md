# TRACK-A-01 Remaining Risks

- AIDE `verify` may remain WARN-only because the committed latest task packet
  has a narrower allowed-path list than the explicit TRACK-A-01 prompt and the
  queue task. The warnings have 0 errors and are documented as scope metadata,
  not product failures.
- TRACK-A-02 still needs to define semantic renderer parity at a more detailed
  field and view-model level.
- Runtime renderers, hosted APIs, public route activation, snapshots, relay,
  native clients, live probes, downloads, accounts, uploads, telemetry, and
  source connectors remain deferred.
