# Q32 Prompt Summary

Implement controlled Eureka synchronization from the canonical Q31 AIDE Lite
Pack exported by `julesc013/aide`.

Required result:

- Import portable commit discipline, changelog preview, task/WorkUnit recovery,
  generic Git workflow policy, dry-run helper commands, tests, golden tasks, and
  docs where safe.
- Preserve Eureka-specific memory, queue/evidence, generated context/reports,
  target-local golden tasks, manual `AGENTS.md` content, and all product source
  boundaries.
- Do not copy AIDE source queue/history, memory, generated context/reports,
  source Git detection/helper plans, `.aide.local/`, secrets, raw prompts, or
  raw responses.
- Do not mutate branches, install hooks, call providers/models/network, or
  change Eureka product behavior.

The full operator prompt was supplied in chat. This summary records the local
task intent without storing raw chat logs.
