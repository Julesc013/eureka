id: LOCAL-09
title: Deterministic local worker runner
status: completed
purpose: Add deterministic local worker execution after operator-gated review/rebuild exists.
allowed_scope_summary: Worker runner only when a future LOCAL-09 task prompt enables it; no LAN, deployment, source probes, extraction, or model/provider calls unless explicitly scoped.
gate:
  - LOCAL-08 completed with operator-gated review/rebuild loop
  - no LAN binding
  - no deployment
  - no production readiness claim
  - no public launch claim
proof_level_required: L2
recommended_after: LOCAL-08
recommended_next: LOCAL-10
completion_evidence:
  - runtime/local/worker package added
  - deterministic worker runner CLI and demo added
  - safe local workers execute through WorkUnits
  - blocked worker kinds fail closed before execution
  - F0 remains deferred until LOCAL-14
