# Eureka AIDE Preservation Plan

Q55 preserved target-owned Eureka state while applying portable AIDE control-plane updates.

Preserved:

- `.aide/memory/**`
- `.aide/queue/**` except the added Q55 packet
- `.aide/context/latest-*` until regenerated by Eureka-local commands
- `.aide/reports/eureka-*`
- Eureka-specific golden tasks
- `AGENTS.md` manual content and existing managed sections
- architecture validators, command matrix, test/eval lanes, source/evidence/index validators, and product safety docs

Excluded from source import:

- source `.aide/queue/**`
- source `.aide/memory/**`
- source generated context/reports/Git helper state
- source release dist archives as target truth
- source local state, secrets, raw prompts, raw responses

Q56 must continue the rule:

`discover -> classify -> wrap -> adapt -> migrate -> retire with evidence`

No deletion, movement, execution, or retirement of Eureka tools is authorized without later reviewed evidence.
