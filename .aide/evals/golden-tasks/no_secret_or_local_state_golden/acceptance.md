# Acceptance

- `.aide.local/`, `.aide.local/**`, `.env`, and Python/cache paths are ignored.
- No `.aide.local/`, `.env`, or `secrets/` paths are tracked.
- Selected AIDE packets, reports, and guidance contain no secret-like values.
- Reports omit raw prompt and raw response bodies.
