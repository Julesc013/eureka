# Acceptance

- AGENTS contains Eureka component boundaries and dependency law.
- Repo map does not inline file contents.
- Repo map surfaces contracts, control, runtime engine, runtime gateway, runtime connectors, surfaces, and AIDE paths.
- Context packet references repo-map, role counts, test-map, and `contents_inline: false`.
- Architecture validation refs exist.
