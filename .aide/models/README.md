# AIDE Advisory Model Metadata

This directory contains portable AIDE Lite routing metadata for Eureka. It is
advisory configuration only and does not authorize provider calls, model calls,
Gateway forwarding, local model setup, model downloads, pricing claims, or
credential storage.

Provider credentials and local runtime state belong only under gitignored
`.aide.local/` if a future reviewed phase explicitly enables them.
