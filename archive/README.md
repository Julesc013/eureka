# Archive

`archive/` retains historical, retired, or quarantined material. It is not an
active source authority and must not be imported by runtime or surface code.

Tools and tests may read archived material as audit evidence when a validator
or migration explicitly allows it.

Use:

```powershell
python scripts/validate_archive_import_guard.py
```
